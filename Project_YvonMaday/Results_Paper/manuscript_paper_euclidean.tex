\documentclass[11pt]{article}
\usepackage[a4paper,margin=1in]{geometry}
\usepackage{amsmath,amssymb,bm}
\usepackage{graphicx}
\usepackage[hypertexnames=false]{hyperref}
\usepackage{booktabs}
\usepackage{float}
\usepackage[section]{placeins}
\usepackage{array}
\setlength{\emergencystretch}{1.5em}

\newcommand{\q}{\mathbf q}
\newcommand{\qbar}{\overline{\mathbf q}}
\newcommand{\qp}{\q}
\newcommand{\qs}{\qbar}
\newcommand{\uvec}{\mathbf u}
\newcommand{\mub}{\bm\mu}
\newcommand{\z}{\mathbf z}
\newcommand{\norm}[1]{\left\lVert #1 \right\rVert}

\title{\vspace{-1.5em}Residual-adaptive correction of parameter--time closures for hyperreduced projection-based models}
\author{S. Ares de Parga, H. Ballout, Y. Maday, H. Hassan}
\date{\today}

\begin{document}
\maketitle
\vspace{-1.0em}

\begin{abstract}
We compare learned closures and nonlinear manifolds for parametric
projection-based reduced-order models of a nonlinear time-dependent system.
All models use a common Euclidean proper-orthogonal-decomposition (POD)
coordinate system.  The learned families comprise \textsc{PROM-ANN} closures,
an intrusive \textsc{PROM-POD-AE} decoder manifold, and the non-intrusive
\textsc{POD-NN-ROM} and \textsc{POD-DL-ROM} maps.  Their main numerical
comparison uses hyperreduced PROMs (HPROMs), with linear-HPROM-generated
training targets and model-specific positive cubature rules.

A parameter--time closure prescribes secondary coordinates without responding
to the current residual.  We augment that tail with a small residual-adaptive
space obtained by eliminating the primary increment in the linearized
least-squares Petrov--Galerkin problem.  The correction directions are rebuilt
at each time step and frozen during its nonlinear solve.  In the hyperreduced
realization, both their construction and the solve use the same weighted
sampled residual.  On a two-dimensional inviscid Burgers benchmark, three
correction directions give a mean in-domain state error of $0.455\%$ with
nine training trajectories, near the $0.441\%$ linear-HPROM reference;
the conventional parameter--time closure gives $1.620\%$.  A separate
common-initialization, equal-dimensional full-residual control supports the
role of direction selection.  Nested HPROM data sets with $9$, $9+8$,
$9+12$, and $9+18$ trajectories show that enrichment improves in-domain
accuracy but need not improve extrapolation monotonically.  The corrected
model remains near the linear reference at every tested data budget.
Validation-based reduction of its cubature support from 3532 to 1506 cells
approximately halves its measured online time.  These observations establish
a practical recovery mechanism for this benchmark, not a general state-error
or extrapolation guarantee.
\end{abstract}

\vspace{-0.8em}

\section{Introduction}
\label{sec:introduction}
Projection-based model order reduction (PMOR) reduces a high-dimensional model
while retaining its governing residual and its parameter dependence
\cite{antoulas2005approximation,quarteroni2014reduced}.  For nonlinear,
transport-dominated systems, an accurate linear subspace may nevertheless
require many coordinates.  Nonlinear trial manifolds, learned closures, and
latent representations seek to reduce the number of online unknowns without
discarding the projection principle
\cite{barnett2023neural,cohen2023nonlinear,aghili2026nonlinear}.

This work compares intrusive closure-enhanced PROMs (PROM--ANN), an intrusive
decoder manifold (PROM--POD--AE), a non-intrusive direct coefficient map
(POD--NN--ROM), and a non-intrusive latent map (POD--DL--ROM).  PROM means
that the full time-discrete residual is minimized online.  It is not
synonymous with a generic ROM: the non-intrusive baselines do not evaluate a
residual online.  Likewise, the HPROM formulation below is a sampled-residual
extension of PROM, not a second name for the same computation.

The reported campaign uses a standard Euclidean POD basis and a common
linear HPROM teacher.  This permits a direct comparison between
a parameter--time-conditioned closure, introduced below as Case~2, which
prescribes a learned secondary-coordinate tail but solves a primary
coordinate block, and POD--NN--ROM, which predicts the full coordinate
vector from the same parameter--time map.  Model-specific ECM rules equip
the conventional intrusive trial maps with hyperreduction.  For the adaptive
correction, a positive moment-fitted rule additionally represents secondary
directions that are selected online rather than prescribed offline.  These
rules approximate the same governing residual but define different weighted
sampled objectives; they are not assumed to be identical optimality problems.

Two responses to limited regression accuracy are examined separately.  The
first adds a small number of residual-solved secondary correction amplitudes
to Case~2, using the existing trained map.  The second enriches the
linear-HPROM coefficient data and retrains all learned families.  The former
spends additional work online; the latter spends additional work offline.
Their separate assessment identifies what can be recovered from the
governing equations before generating further training trajectories.
Full-residual PROM experiments are retained as diagnostic controls in the
appendices.  They isolate correction rank and equal-dimensional trial-space
selection before introducing cubature.  The main results instead assess
HPROM accuracy, hyperreduction, online work, and coefficient-data enrichment.

\section{Projection Framework}
\label{sec:projection-framework}
\subsection{Time-discrete residual and Euclidean POD basis}
Consider a parametric nonlinear semi-discrete initial-value problem
\begin{equation}
\mathbf M(\mub)\dot{\uvec}(t;\mub)+\mathbf f(\uvec(t;\mub);\mub)
-\mathbf g(t;\mub)=\mathbf0,\qquad \uvec(0;\mub)=\uvec_0(\mub),
\label{eq:ivp}
\end{equation}
with $\mub\in\mathcal D$.  After a chosen implicit time discretization, let
$\mathbf r^{m+1}(\uvec;\mub)\in\mathbb R^N$ denote the residual at step
$m+1$, with dependence on the previous state understood.  The HDM solves
\begin{equation}
\mathbf r^{m+1}(\uvec^{m+1};\mub)=\mathbf0.
\label{eq:discrete-residual}
\end{equation}
Let $\uvec_{\mathrm{ref}}$ be the snapshot mean and let $\mathbf S_c$ collect
the centered HDM snapshots.  The thin singular-value decomposition
\begin{equation}
\mathbf S_c=\mathbf U\boldsymbol\Sigma\mathbf Y^T
\end{equation}
defines the Euclidean POD basis retained in this work,
\begin{equation}
\mathbf V_{\mathrm{tot}}=\mathbf U(:,1:n_{\mathrm{tot}}),\qquad
\mathbf V_{\mathrm{tot}}^T\mathbf V_{\mathrm{tot}}=\mathbf I.
\label{eq:euclidean-pod}
\end{equation}
The common affine linear trial manifold is
\begin{equation}
\boldsymbol\phi_N(\q_N)=\uvec_{\mathrm{ref}}+
\mathbf V_{\mathrm{tot}}\q_N,\qquad
\q_N\in\mathbb R^{n_{\mathrm{tot}}}.
\label{eq:linear-reconstruction}
\end{equation}
All learned trial maps below are expressed in these same coordinates.

\subsection{Linear PROM and LSPG reference}
The full-residual linear PROM is the time-discrete least-squares
Petrov--Galerkin (LSPG) problem
\begin{equation}
\q_N^{m+1}(\mub)=\arg\min_{\mathbf x\in\mathbb R^{n_{\mathrm{tot}}}}
\norm{\mathbf r^{m+1}(\boldsymbol\phi_N(\mathbf x);\mub)}_2^2.
\label{eq:linear-prom}
\end{equation}
At Gauss--Newton iterate $k$, define
\begin{equation}
\mathbf T_N=\mathbf V_{\mathrm{tot}},\qquad
\mathbf J^{m+1,k}=
\left.\frac{\partial\mathbf r^{m+1}}{\partial\uvec}\right|_{
\boldsymbol\phi_N(\q_N^{m+1,k})},\qquad
\mathbf W_N^{m+1,k}=\mathbf J^{m+1,k}\mathbf T_N.
\end{equation}
The normal equations for the increment are
\begin{equation}
\left(\mathbf W_N^{m+1,k}\right)^T\mathbf W_N^{m+1,k}
\Delta\q_N^{m+1,k}=-
\left(\mathbf W_N^{m+1,k}\right)^T
\mathbf r^{m+1}\!\left(\boldsymbol\phi_N(\q_N^{m+1,k});\mub\right).
\label{eq:linear-prom-gn}
\end{equation}
Equation~\eqref{eq:linear-prom} generates all training targets in the PROM
campaign.

\subsection{Linear HPROM and ECM formulation}
\label{sec:linear-hprom}
The linear HPROM replaces full residual evaluations in the LSPG update by a
sparse, positive empirical-cubature approximation.  This formulation is
given in detail because the same projected-residual algebra is used when the
affine trial manifold is replaced by a learned one.  Let
$\mathcal E=\{e_i\}_{i=1}^{N_e}$ be the mesh entities.  The time-discrete
residual is assembled from local contributions as
\begin{equation}
\mathbf r^{m+1}(\uvec;\mub)=\sum_{e_i\in\mathcal E}\mathbf L_{e_i}^T
\mathbf r_{e_i}^{m+1}(\mathbf L_{e_i^+}\uvec;\mub).
\label{eq:entity-residual}
\end{equation}
Here $\mathbf r_{e_i}^{m+1}\in\mathbb R^{d_{e_i}}$ is the contribution of
entity $e_i$, $\mathbf L_{e_i}\in\{0,1\}^{d_{e_i}\times N}$ extracts its
residual degrees of freedom, and $\mathbf L_{e_i^+}$ extracts the enlarged
state stencil needed to evaluate it.  The stencil contains neighbouring
entities whenever the discretization requires them.

For the affine trial map \eqref{eq:linear-reconstruction}, the projected
LSPG residual at a Gauss--Newton iterate has the entity-wise form
\begin{equation}
\begin{aligned}
\mathbf r_N^{m+1,k}(\mub)
&:=\left(\mathbf W_N^{m+1,k}\right)^T
\mathbf r^{m+1}\!\left(\boldsymbol\phi_N(\q_N^{m+1,k});\mub\right)\\
&=\sum_{e_i\in\mathcal E}
\left(\mathbf L_{e_i}\mathbf W_N^{m+1,k}\right)^T
\mathbf r_{e_i}^{m+1}\!\left(
\mathbf L_{e_i^+}\boldsymbol\phi_N(\q_N^{m+1,k});\mub\right).
\end{aligned}
\label{eq:linear-projected-residual}
\end{equation}
Let $\mathbf J_{e_i}^{m+1,k}$ be the derivative of the local residual with
respect to the stencil state at the same iterate.  The Gauss--Newton matrix
is assembled analogously,
\begin{equation}
\mathbf J_N^{m+1,k}:=
\left(\mathbf W_N^{m+1,k}\right)^T\mathbf J^{m+1,k}\mathbf T_N
=\sum_{e_i\in\mathcal E}
\left(\mathbf L_{e_i}\mathbf W_N^{m+1,k}\right)^T
\mathbf J_{e_i}^{m+1,k}\left(\mathbf L_{e_i^+}\mathbf T_N\right).
\label{eq:linear-projected-jacobian}
\end{equation}

ECM selects a reduced entity set $\widetilde{\mathcal E}_N\subset\mathcal E$
and positive weights $\xi_{N,e_i}$.  The same rule approximates both
projected quantities,
\begin{equation}
\begin{aligned}
\widehat{\mathbf r}_N^{m+1,k}
&=\sum_{e_i\in\widetilde{\mathcal E}_N}\xi_{N,e_i}
\left(\mathbf L_{e_i}\mathbf W_N^{m+1,k}\right)^T
\mathbf r_{e_i}^{m+1}\!\left(
\mathbf L_{e_i^+}\boldsymbol\phi_N(\q_N^{m+1,k});\mub\right),\\
\widehat{\mathbf J}_N^{m+1,k}
&=\sum_{e_i\in\widetilde{\mathcal E}_N}\xi_{N,e_i}
\left(\mathbf L_{e_i}\mathbf W_N^{m+1,k}\right)^T
\mathbf J_{e_i}^{m+1,k}\left(\mathbf L_{e_i^+}\mathbf T_N\right).
\end{aligned}
\label{eq:linear-ecm-sampled-normal-equations}
\end{equation}
The online sampled update is consequently
\begin{equation}
\widehat{\mathbf J}_N^{m+1,k}\Delta\q_N^{m+1,k}
=-\widehat{\mathbf r}_N^{m+1,k},\qquad
\q_N^{m+1,k+1}=\q_N^{m+1,k}+\Delta\q_N^{m+1,k}.
\label{eq:linear-hprom-gn-update}
\end{equation}

\paragraph{Offline cubature construction.}
For every selected parameter--time training state $s$, the linear coordinate
$\q_{N,s}$ and its LSPG test basis $\mathbf W_{N,s}$ define the contribution
of entity $e_i$ to the projected residual,
\begin{equation}
\mathbf c_{N,e_i}^{(s)}=(\mathbf L_{e_i}\mathbf W_{N,s})^T
\mathbf r_{e_i}(\mathbf L_{e_i^+}\boldsymbol\phi_N(\q_{N,s});\mub_s).
\label{eq:linear-ecm-contribution}
\end{equation}
Stacking all $n_{\mathrm{tot}}$-component contributions over
$N_{\mathrm{ECM}}$ selected states gives
\begin{equation}
\mathbf C_N=
\begin{bmatrix}
\mathbf c_{N,e_1}^{(1)} & \cdots & \mathbf c_{N,e_{N_e}}^{(1)}\\
\vdots & & \vdots\\
\mathbf c_{N,e_1}^{(N_{\mathrm{ECM}})} & \cdots &
\mathbf c_{N,e_{N_e}}^{(N_{\mathrm{ECM}})}
\end{bmatrix}
\in\mathbb R^{n_{\mathrm{tot}}N_{\mathrm{ECM}}\times N_e},
\qquad \mathbf b_N=\mathbf C_N\mathbf1.
\label{eq:linear-ecm-training-matrix}
\end{equation}
ECM seeks a sparse nonnegative approximation of the full projected-residual
data,
\begin{equation}
\boldsymbol\xi_N=\arg\min_{\boldsymbol\zeta\geq\mathbf0}
\norm{\mathbf C_N\boldsymbol\zeta-\mathbf b_N}_2^2,\qquad
\widetilde{\mathcal E}_N=\{e_i\in\mathcal E:\xi_{N,e_i}>0\}.
\label{eq:linear-ecm}
\end{equation}
In the present implementation, a direct SVD removes numerically null
directions of $\mathbf C_N$ before the nonnegative cubature solve.  The rule
is therefore tied to the affine trial manifold and its LSPG test basis.  In
particular, replacing $\boldsymbol\phi_N$ by a learned closure or decoder
also changes the tangent, the test basis, and the projected contributions;
the corresponding ECM rule must be constructed for that map.

\paragraph{Relation to other hyperreduction methods.}
The common step is to approximate an entity-wise sum of \emph{projected}
residual contributions, rather than the state reconstruction or a learned
map.  We use ECM in the sense of empirical cubature
\cite{hernandez2017dimensional,hernandez2020multiscale}.  ECSW is another
project--then--approximate hyperreduction strategy
\cite{farhat2015structure}.  It could be used with the same LSPG and trial
manifold formulations, but ECM is the rule used in this work.

\section{Learned Reduced Representations}
\label{sec:learned-representations}
\subsection{PROM--ANN closures}
Partition the common POD basis and coordinate vector as
\begin{equation}
\mathbf V_{\mathrm{tot}}=[\mathbf V\ \overline{\mathbf V}],\qquad
\q_N=\begin{bmatrix}\qp\\\qs\end{bmatrix},\qquad
n+\bar n=n_{\mathrm{tot}}.
\label{eq:partition}
\end{equation}
Here $\q\in\mathbb R^n$ and $\qbar\in\mathbb R^{\bar n}$ denote the
primary and secondary coordinates, respectively.  The Euclidean POD
construction gives
\begin{equation}
\mathbf V^T\mathbf V=\mathbf I_n,\qquad
\overline{\mathbf V}^{\,T}\overline{\mathbf V}=\mathbf I_{\bar n},\qquad
\mathbf V^T\overline{\mathbf V}=\mathbf0,
\label{eq:partition-orthogonality}
\end{equation}
and the associated linear state is
\begin{equation}
\uvec=\uvec_{\mathrm{ref}}+\mathbf V\q+
\overline{\mathbf V}\qbar.
\label{eq:partitioned-state}
\end{equation}
The full-coordinate linear PROM solves both blocks.  A learned closure
instead supplies the secondary block and leaves only the primary block as
an online unknown.
PROM--ANN supplies the secondary block through a learned relation
\begin{equation}
\qs=\mathcal F(\qp,\mub,t),\qquad
\boldsymbol\phi_{\mathcal F}(\qp;\mub,t)=\uvec_{\mathrm{ref}}+
\mathbf V\qp+\overline{\mathbf V}\mathcal F(\qp,\mub,t).
\label{eq:closure-manifold}
\end{equation}
The network provides reduced secondary coordinates, not a direct HDM state
approximation.  The primary coordinates remain online unknowns of a residual
minimization.  This is the neural-network-augmented projection construction
of \cite{barnett2023neural}; analogous closure roles have also been
represented by non-neural regressors \cite{ares2026closure}.

The three cases are information patterns in this common construction:
\begin{align}
\text{Case 1:}\quad&\qs=\mathcal N(\qp),&
\text{Case 2:}\quad&\qs=\mathcal M(\mub,t),&
\text{Case 3:}\quad&\qs=\mathcal H(\qp,\mub,t).
\label{eq:closure-cases}
\end{align}
Case~1 is state-conditioned, Case~2 parameter--time-conditioned, and Case~3
combines both inputs.  The LSPG problem is
\begin{equation}
\qp^{m+1}(\mub)=\arg\min_{\mathbf x\in\mathbb R^n}
\norm{\mathbf r^{m+1}(\boldsymbol\phi_{\mathcal F}
(\mathbf x;\mub,t^{m+1});\mub)}_2^2,
\label{eq:closure-lspg}
\end{equation}
with tangent
\begin{equation}
\mathbf T_{\mathcal F}=\mathbf V+\overline{\mathbf V}
\frac{\partial\mathcal F}{\partial\qp},\qquad
\mathbf W_{\mathcal F}=\mathbf J\mathbf T_{\mathcal F}.
\label{eq:closure-tangent}
\end{equation}
In particular, at a Gauss--Newton iterate the coordinate increment solves
\begin{equation}
\Delta\q=\arg\min_{\mathbf d\in\mathbb R^n}
\norm{\mathbf r+\mathbf J\mathbf T_{\mathcal F}\mathbf d}_2^2,
\qquad
\mathbf W_{\mathcal F}^T\mathbf W_{\mathcal F}\Delta\q
=-\mathbf W_{\mathcal F}^T\mathbf r.
\label{eq:closure-gn}
\end{equation}
The residual, Jacobian, and tangent are all evaluated at that iterate;
the previous time-discrete state remains fixed during the nonlinear solve.
The normal equations express the optimality condition, while the numerical
implementation solves the least-squares system directly.
For Case~2, $\partial\mathcal M/\partial\qp=\mathbf0$ and
$\mathbf T_{\mathcal M}=\mathbf V$.  Its tangent is therefore the cheapest,
but its prescribed secondary block is not corrected through a
state-dependent tangent.

\subsection{PROM--POD--AE and intrusive trial maps}
An autoencoder maps the complete linear coordinate vector through a latent
variable,
\begin{equation}
\z=\mathcal E(\q_N),\qquad
\widehat{\q}_N=\mathcal D(\z),\qquad \z\in\mathbb R^{n_z}.
\end{equation}
PROM--POD--AE uses the decoder as an intrusive nonlinear trial manifold,
\begin{equation}
\boldsymbol\phi_{\mathrm{AE}}(\z)=\uvec_{\mathrm{ref}}+
\mathbf V_{\mathrm{tot}}\mathcal D(\z),\qquad
\mathbf T_{\mathrm{AE}}=\mathbf V_{\mathrm{tot}}
\frac{\partial\mathcal D}{\partial\z}.
\label{eq:podae-manifold}
\end{equation}
Its latent coordinate minimizes the same residual as
\eqref{eq:closure-lspg}, but over $\z$ rather than $\qp$.  This is distinct
from POD--DL--ROM, which is non-intrusive.

\begin{table}[H]
\centering
\caption{Trial decoders and tangent bases of the intrusive models.  PROMs
minimize the full time-discrete residual; their HPROM counterparts use
map-specific weighted sampled residuals with these same decoders and tangents.}
\label{tab:trial-maps}
\small
\setlength{\tabcolsep}{4pt}
\renewcommand{\arraystretch}{1.25}
\begin{tabular}{@{}l c l l@{}}
\toprule
Model & Online variable & Trial decoder & Tangent \\
\midrule
Linear PROM/HPROM & $\q_N$ & $\uvec_{\mathrm{ref}}+\mathbf V_{\mathrm{tot}}\q_N$ & $\mathbf V_{\mathrm{tot}}$ \\
PROM--ANN Case 1 & $\qp$ & $\uvec_{\mathrm{ref}}+\mathbf V\qp+\overline{\mathbf V}\mathcal N(\qp)$ & $\mathbf V+\overline{\mathbf V}\,\partial\mathcal N/\partial\qp$ \\
PROM--ANN Case 2 & $\qp$ & $\uvec_{\mathrm{ref}}+\mathbf V\qp+\overline{\mathbf V}\mathcal M(\mub,t)$ & $\mathbf V$ \\
PROM--ANN Case 3 & $\qp$ & $\uvec_{\mathrm{ref}}+\mathbf V\qp+\overline{\mathbf V}\mathcal H(\qp,\mub,t)$ & $\mathbf V+\overline{\mathbf V}\,\partial\mathcal H/\partial\qp$ \\
PROM--POD--AE & $\z$ & $\uvec_{\mathrm{ref}}+\mathbf V_{\mathrm{tot}}\mathcal D(\z)$ & $\mathbf V_{\mathrm{tot}}\,\partial\mathcal D/\partial\z$ \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Model-specific ECM construction}
\label{sec:nonlinear-ecm}
The HPROM extension does not reuse the linear rule in \eqref{eq:linear-ecm}.
For any intrusive map $\boldsymbol\phi_c$ and tangent $\mathbf T_c$, the
entity contributions use $\mathbf W_c=\mathbf J\mathbf T_c$.  A manifold
coordinate is first fitted at every selected HDM state,
\begin{equation}
\mathbf y_{c,s}=\arg\min_{\mathbf y}\norm{\uvec_s^{m_s+1}-
\boldsymbol\phi_c(\mathbf y;\mub_s,t^{m_s+1})}_2^2.
\label{eq:nonlinear-manifold-fit}
\end{equation}
For Case~2 this is linear least squares after subtracting the prescribed tail,
\begin{equation}
{\qp}_{2,s}=\mathbf V^\dagger\left[
\uvec_s^{m_s+1}-\uvec_{\mathrm{ref}}-
\overline{\mathbf V}\mathcal M(\mub_s,t^{m_s+1})\right].
\label{eq:case2-ecm-fit}
\end{equation}
For Cases~1 and~3 and for the decoder manifold, it is nonlinear least
squares, for example with the Gauss--Newton update
\begin{equation}
\mathbf y_{c,s}^{(k+1)}=\mathbf y_{c,s}^{(k)}-
\left[\frac{\partial\boldsymbol\phi_c}{\partial\mathbf y}\right]^\dagger
\left[\boldsymbol\phi_c(\mathbf y_{c,s}^{(k)};\mub_s,t^{m_s+1})-
\uvec_s^{m_s+1}\right].
\label{eq:nonlinear-manifold-fit-gn}
\end{equation}
At the fitted state,
\begin{equation}
\mathbf c_{c,e_i}^{(s)}=(\mathbf L_{e_i}\mathbf W_{c,s})^T
\mathbf r_{e_i}(\mathbf L_{e_i^+}
\boldsymbol\phi_c(\mathbf y_{c,s};\mub_s,t^{m_s+1});\mub_s).
\label{eq:nonlinear-ecm-contribution}
\end{equation}
Stacking these vectors produces $\mathbf C_c$ and
$\mathbf b_c=\mathbf C_c\mathbf1$ as in \eqref{eq:linear-ecm}; a positive
ECM rule is then computed for each map.  The trained closure or decoder, its
tangent, and the parameter--time sample set are therefore part of a rule.  A
linear-HPROM rule, or one built for a different nonlinear map, is not assumed
valid without an additional consistency assessment
\cite{hernandez2017dimensional,hernandez2020multiscale,de2023hyper}.

\subsection{Non-intrusive reference models}
POD--NN--ROM is a direct map
\begin{equation}
\q_N(t;\mub)\approx\mathcal G_q(\mub,t),
\label{eq:podnn}
\end{equation}
and does not solve a residual online.  POD--DL--ROM uses a learned latent map
and decoder,
\begin{equation}
\z(t;\mub)\approx\mathcal G_z(\mub,t),\qquad
\q_N(t;\mub)\approx\mathcal D(\mathcal G_z(\mub,t)),
\label{eq:poddl}
\end{equation}
following the POD--DL--ROM principle \cite{fresca2022pod}.  These are direct
reduced-coordinate regressors, not PROM residual solves.

\section{Residual-Adaptive Secondary Correction for Case~2}
\label{sec:adaptive-case2}
\subsection{From a prescribed tail to a corrected trial space}
In Case~2, the network fixes $\qbar=\mathcal M(\mub,t)$ and LSPG adjusts
only $\q$.  At a fixed linearization, primary increments change the residual
only within $\operatorname{range}(\mathbf J\mathbf V)$.  The component of a
secondary prediction error's linearized residual contribution orthogonal to
that space cannot be removed by primary increments alone.  The proposed extension retains
the trained map and permits selected secondary corrections online.

At step $m+1$, let $\mathbf B_r^{m+1}\in\mathbb R^{\bar n\times r}$
contain $r$ orthonormal directions in secondary-coordinate space, where
$0\leq r\leq\bar n$.  Their construction is given below.  Introduce
$r$ additional amplitudes $\mathbf a\in\mathbb R^r$ and define
\begin{equation}
\begin{aligned}
\qbar&=\mathcal M(\mub,t^{m+1})+\mathbf B_r^{m+1}\mathbf a,\\
\boldsymbol\phi_r^{m+1}(\q,\mathbf a;\mub)
&=\uvec_{\mathrm{ref}}+\mathbf V\q+
\overline{\mathbf V}
\left[\mathcal M(\mub,t^{m+1})+\mathbf B_r^{m+1}\mathbf a\right].
\end{aligned}
\label{eq:adaptive-trial}
\end{equation}
Each column of $\mathbf B_r^{m+1}$ is a pattern over all $\bar n$
secondary coordinates.  Thus $r$ counts independent correction patterns,
not selected POD coefficients.  A single amplitude can modify every
secondary coordinate through its corresponding column.

The complete coordinate vector retains the notation of the linear
reference:
\begin{equation}
\q_N^{m+1}=\begin{bmatrix}
\q^{m+1}\\
\mathcal M(\mub,t^{m+1})+\mathbf B_r^{m+1}\mathbf a^{m+1}
\end{bmatrix}\in\mathbb R^{n_{\mathrm{tot}}},\qquad
\uvec_r^{m+1}=\uvec_{\mathrm{ref}}+\mathbf V_{\mathrm{tot}}\q_N^{m+1}.
\label{eq:adaptive-complete-coordinates}
\end{equation}
Thus $\q_N$ always denotes the complete reconstructed reduced-coordinate
vector; $[\q;\mathbf a]$ contains only the independent online unknowns.

Once $\mathbf B_r^{m+1}$ has been constructed and held fixed, the map
\eqref{eq:adaptive-trial} is affine in its $n+r$ unknowns.  Its exact tangent
is
\begin{equation}
\mathbf T_r^{m+1}=\left[\mathbf V\quad
\overline{\mathbf V}\mathbf B_r^{m+1}\right],\qquad
(\mathbf T_r^{m+1})^T\mathbf T_r^{m+1}=\mathbf I_{n+r}.
\label{eq:adaptive-tangent}
\end{equation}
The second identity follows from \eqref{eq:partition-orthogonality} and
$(\mathbf B_r^{m+1})^T\mathbf B_r^{m+1}=\mathbf I_r$.  It also gives
$\norm{\overline{\mathbf V}\mathbf B_r^{m+1}\mathbf a}_2
=\norm{\mathbf a}_2$: the amplitude norm measures the state correction in
the Euclidean norm already used by the POD basis.

\subsection{Eliminating the primary increment}
The correction directions should address residual components that the
primary update cannot remove.  To identify them, first form the predictor
\begin{equation}
\uvec_*^{m+1}=\uvec_{\mathrm{ref}}+\mathbf V\q^m+
\overline{\mathbf V}\mathcal M(\mub,t^{m+1}).
\label{eq:adaptive-predictor}
\end{equation}
The residual at this predictor uses the previous \emph{computed} state
$\uvec_r^m$ in its time-discretization terms.  No reference trajectory enters
that evaluation.  In the derivation that follows, suppress $m+1$ and
$\mub$ and write
\begin{equation}
\mathbf r_* = \mathbf r^{m+1}(\uvec_*^{m+1};\mub),\qquad
\mathbf J_* = \left.\frac{\partial\mathbf r^{m+1}}{\partial\uvec}
\right|_{\uvec_*^{m+1}},\qquad \mathbf A=\mathbf J_*\mathbf V.
\label{eq:adaptive-linearization}
\end{equation}
If both coordinate blocks were free, the linearized correction would solve
\begin{equation}
\min_{\Delta\q\in\mathbb R^n,\,\Delta\qbar\in\mathbb R^{\bar n}}
\norm{\mathbf r_*+\mathbf A\Delta\q+
\mathbf J_*\overline{\mathbf V}\Delta\qbar}_2^2.
\label{eq:adaptive-full-linearized}
\end{equation}
Assume that $\mathbf A$ has full column rank and take its thin QR
factorization $\mathbf A=\mathbf Q\mathbf R_A$, with
$\mathbf Q^T\mathbf Q=\mathbf I_n$.  Define the orthogonal projector
\begin{equation}
\mathbf P=\mathbf I_N-\mathbf Q\mathbf Q^T.
\label{eq:adaptive-projector}
\end{equation}
For a fixed $\Delta\qbar$, least squares eliminates the primary increment:
\begin{equation}
\Delta\q(\Delta\qbar)=-\mathbf A^\dagger
\left(\mathbf r_*+\mathbf J_*\overline{\mathbf V}\Delta\qbar\right).
\label{eq:adaptive-primary-elimination}
\end{equation}
Substitution into \eqref{eq:adaptive-full-linearized} leaves precisely
\begin{equation}
\min_{\Delta\qbar\in\mathbb R^{\bar n}}
\norm{\mathbf P\mathbf r_*+\mathbf C\Delta\qbar}_2^2,
\qquad \mathbf C=\mathbf P\mathbf J_*\overline{\mathbf V}.
\label{eq:adaptive-secondary-ls}
\end{equation}
The projector is essential: a direction is assessed by its effect on the
residual \emph{after} the best primary correction, rather than by its effect
on a residual component already removable through $\mathbf V$.

Define the symmetric positive-semidefinite operator and right-hand side
\begin{equation}
\mathbf H=\mathbf C^T\mathbf C,\qquad
\mathbf g=\mathbf C^T\mathbf P\mathbf r_*
=\overline{\mathbf V}^{\,T}\mathbf J_*^T\mathbf P\mathbf r_*.
\label{eq:adaptive-schur}
\end{equation}
Then the unconstrained secondary normal equations are
$\mathbf H\Delta\qbar=-\mathbf g$.  Equivalently, $\mathbf H$ is the
Schur complement obtained by eliminating the primary block of the full
linearized normal equations.  It is positive definite if
$\mathbf J_*\mathbf V_{\mathrm{tot}}$ has full column rank.  These
operators are derived from the current Euclidean LSPG problem; they do not
alter the POD basis or the offline training targets.

\subsection{A small Krylov space for the secondary correction}
Solving \eqref{eq:adaptive-secondary-ls} for all $\bar n$ coordinates
would undo the reduction achieved by the closure.  Instead, for $r\geq1$
and before any Krylov breakdown, choose
\begin{equation}
\operatorname{range}(\mathbf B_r)=\mathcal K_r(\mathbf H,\mathbf g)
:=\operatorname{span}\{\mathbf g,\mathbf H\mathbf g,\ldots,
\mathbf H^{r-1}\mathbf g\},\qquad \mathbf B_r^T\mathbf B_r=\mathbf I_r.
\label{eq:adaptive-krylov}
\end{equation}
Here $r$ is a user-selected computational budget.  Three directions are
one possible choice, not a restriction of the formulation.  The construction
uses orthogonalized operator applications, so it does not explicitly form
powers of $\mathbf H$.

For $r=1$ and $\mathbf g\ne\mathbf0$, the space is the span of
the negative gradient of one half of the objective in
\eqref{eq:adaptive-secondary-ls} at zero secondary increment.  The
linearized minimizer along this direction is
\begin{equation}
\Delta\qbar_1=-\frac{\mathbf g^T\mathbf g}
{\mathbf g^T\mathbf H\mathbf g}\,\mathbf g.
\label{eq:adaptive-rank-one}
\end{equation}
Indeed, inserting $\Delta\qbar=-\alpha\mathbf g$ in
\eqref{eq:adaptive-secondary-ls} and differentiating with respect to
$\alpha$ gives \eqref{eq:adaptive-rank-one}.  Because
$\mathbf g\in\operatorname{range}(\mathbf C^T)=\ker(\mathbf C)^\perp$,
a nonzero $\mathbf g$ satisfies
$\mathbf g^T\mathbf H\mathbf g=\norm{\mathbf C\mathbf g}_2^2>0$;
no assumption that the residual can be reduced to zero is needed.
Higher ranks permit an optimal combination of additional directions
$\mathbf H\mathbf g,\mathbf H^2\mathbf g,\ldots$ in the same
linearized problem.  For a fixed predictor their spaces are nested, hence
\begin{equation}
\min_{\mathbf a\in\mathbb R^{r+1}}
\norm{\mathbf P\mathbf r_*+\mathbf C\mathbf B_{r+1}\mathbf a}_2
\leq
\min_{\mathbf a\in\mathbb R^r}
\norm{\mathbf P\mathbf r_*+\mathbf C\mathbf B_r\mathbf a}_2.
\label{eq:adaptive-nested-bound}
\end{equation}
This is a statement about the linearized residual at the same predictor.
It is not an error bound against the HDM and does not imply monotone
trajectory accuracy as $r$ increases.

The operator actions can be evaluated without assembling either
$\mathbf C$ or the $N\times\bar n$ product
$\mathbf J_*\overline{\mathbf V}$:
\begin{equation}
\begin{aligned}
\mathbf P\mathbf y&=\mathbf y-\mathbf Q(\mathbf Q^T\mathbf y),\\
\mathbf H\mathbf v&=\overline{\mathbf V}^{\,T}\mathbf J_*^T
\mathbf P\left[\mathbf J_*(\overline{\mathbf V}\mathbf v)\right].
\end{aligned}
\label{eq:adaptive-actions}
\end{equation}
The implementation constructs the orthonormal columns by Arnoldi
orthogonalization with two reorthogonalization passes.  If $\mathbf g$
is numerically zero, no secondary direction is added.  If a later vector
is numerically dependent, the construction stops and uses the attained
rank $r_m\leq r$.  In that event all formulas use $r_m$ in place of $r$;
a request for $r$ directions does not require adding null or dependent ones.

\subsection{Nonlinear solve on the frozen affine space}
The linearized problem is used to \emph{select directions}.  The accepted
time-step solution is obtained by minimizing the original nonlinear
time-discrete residual on \eqref{eq:adaptive-trial}:
\begin{equation}
\begin{bmatrix}\q^{m+1}\\\mathbf a^{m+1}\end{bmatrix}
=\arg\min_{\mathbf x\in\mathbb R^n,\,\mathbf b\in\mathbb R^{r_m}}
\norm{\mathbf r^{m+1}
(\boldsymbol\phi_r^{m+1}(\mathbf x,\mathbf b;\mub);\mub)}_2^2.
\label{eq:adaptive-nonlinear-lspg}
\end{equation}
Writing $\mathbf z=[\q;\mathbf a]$, the Gauss--Newton increment is
\begin{equation}
\Delta\mathbf z^{m+1,k}
=\arg\min_{\mathbf d\in\mathbb R^{n+r_m}}
\norm{\mathbf r^{m+1,k}+
\mathbf J^{m+1,k}\mathbf T_r^{m+1}\mathbf d}_2^2.
\label{eq:adaptive-gn}
\end{equation}
Although $\mathbf J^{m+1,k}$ changes with the iterate,
$\mathbf B_r^{m+1}$ and $\mathbf T_r^{m+1}$ remain fixed throughout
this solve.  Consequently \eqref{eq:adaptive-tangent} is the exact
derivative of the trial map being optimized.  The method does not omit a
derivative of a state-dependent $\mathbf B_r$: constructing the space and
solving on that fixed space are two distinct operations.

The complete online step is as follows.
\begin{enumerate}
\item Predict $\mathcal M(\mub,t^{m+1})$ with the trained network and form
the predictor \eqref{eq:adaptive-predictor} from the preceding primary
coordinates $\q^m$.
\item Evaluate $\mathbf r_*$ and $\mathbf J_*$ using the preceding
computed full reduced state $\uvec_r^m$ in the time-discrete residual.
\item Factor $\mathbf J_*\mathbf V$, apply
\eqref{eq:adaptive-actions}, and construct up to $r$ columns of
$\mathbf B_r^{m+1}$ according to \eqref{eq:adaptive-krylov}.
\item Form $\mathbf T_r^{m+1}$ and initialize the nonlinear solve with
$\mathbf z^{m+1,0}=[\q^m;\mathbf0]$.  Solve
\eqref{eq:adaptive-nonlinear-lspg} by \eqref{eq:adaptive-gn}.
\item Store $\q^{m+1}$ and the corrected secondary coordinates
$\qbar^{m+1}=\mathcal M(\mub,t^{m+1})+
\mathbf B_r^{m+1}\mathbf a^{m+1}$.  Their reconstructed state is the
predecessor used at the next time step.
\end{enumerate}
At the next step a new correction space is constructed.  Its amplitudes
are initialized to zero because the columns generally differ from those at
the previous step; the previous correction is nevertheless retained through
the time-discrete predecessor state.

\subsection{Interpretation, limiting cases, and cost}
The method changes the online trial space while retaining the trained
parameter--time map.  Increasing $n$ would instead free additional individual
POD coordinates.  Increasing $r$ keeps the same primary/secondary split and
frees combinations of secondary coordinates selected by the current
residual and Jacobian.  The two choices can therefore give different
accuracy at the same number of online unknowns.

More precisely, consider the equal-dimensional choice $d=n+r$.  A
conventional Case--2 model with $d$ primary coordinates has tangent space
\begin{equation}
\mathcal T_d^{\mathrm{POD}}
=\operatorname{range}\!\left(\mathbf V_{1:d}\right)
=\operatorname{span}\{\mathbf v_1,\ldots,\mathbf v_{n+r}\},
\label{eq:adaptive-pod-control-space}
\end{equation}
whereas the residual-adaptive model has
\begin{equation}
\mathcal T_{n,r}^{\mathrm{RA}}
=\operatorname{range}\!\left(
\left[\mathbf V_{1:n}\quad
\overline{\mathbf V}_{n}\mathbf B_r\right]\right).
\label{eq:adaptive-equal-dimension-space}
\end{equation}
Both spaces have dimension $n+r$ when the requested rank is attained, but
they are generally different subspaces.  Equation
\eqref{eq:adaptive-pod-control-space} adds the next $r$ POD modes according
to offline snapshot energy.  Equation
\eqref{eq:adaptive-equal-dimension-space} instead adds $r$ combinations
within the complete secondary space according to the current projected
linearized residual problem.  An equal-dimensional numerical control is
therefore required to distinguish the effect of direction selection from
the effect of merely adding unknowns.

For $r=0$, \eqref{eq:adaptive-trial} is the original Case--2 map, and its
online trajectory is recovered when the initialization and nonlinear solver
settings also match.  If the constructed columns span the entire secondary
space, the corrected affine space equals the full linear-PROM space:
\begin{equation}
\left\{\boldsymbol\phi_{\bar n}^{m+1}(\q,\mathbf a;\mub)\right\}
=\uvec_{\mathrm{ref}}+\operatorname{range}(\mathbf V_{\mathrm{tot}}).
\label{eq:adaptive-full-space}
\end{equation}
The network offset then lies inside the freely variable linear space.
Equality of converged trajectories additionally requires compatible initial
states and selection of the same time-step minimizers.  A smaller Krylov
space can solve the predictor's linearized problem exactly upon breakdown,
without spanning the full secondary space; that fact does not make the
subsequent nonlinear problems identical.

At a fixed predecessor, the uncorrected Case--2 space is contained in the
corrected space by taking $\mathbf a=\mathbf0$.  Its global residual
minimum therefore cannot increase upon enlargement.  In practice,
Gauss--Newton is stopped at finite tolerances, and different trajectories
produce different predecessor states.  Neither space inclusion nor
\eqref{eq:adaptive-nested-bound} guarantees lower state error over a full
trajectory, nonlinear convergence, or superiority to Cases~1 and~3.

Finally, $n+r_m$ unknowns do not imply the cost of a fixed
$(n+r_m)$-mode linear PROM.  Construction requires the full residual and
Jacobian, a QR factorization of $\mathbf J_*\mathbf V$, and actions
involving all $\bar n$ secondary modes.  The final nonlinear solves use
$\mathbf J\mathbf T_r$, whose width is only $n+r_m$, and need no
network derivative with respect to the primary coordinates.  Both the
direction-construction cost and the nonlinear solve must be included in
online timings.  The next subsection gives a weighted sampled realization
of this construction.  It reduces the number of residual rows and state
stencil entries, but still acts on all secondary coordinate directions when
building the correction space.

\subsection{Weighted sampled realization}
\label{sec:adaptive-sampling}
Let a fixed positive cubature rule select residual rows and scale them by
square roots of its weights.  Denote this operation by
$\mathbf S_\xi\in\mathbb R^{N_\xi\times N}$ and set
$\mathbf D_\xi=\mathbf S_\xi^T\mathbf S_\xi$.  The HPROM time-step
objective is
\begin{equation}
\min_{\q,\mathbf a}
\norm{\mathbf S_\xi\mathbf r^{m+1}
(\boldsymbol\phi_r^{m+1}(\q,\mathbf a;\mub);\mub)}_2^2.
\label{eq:adaptive-sampled-objective}
\end{equation}
Each selected Burgers cell contributes both field residuals, so
$N_\xi=2|\widetilde{\mathcal E}_\xi|$.  Computing these residual rows
requires state values on their finite-volume stencil, not the complete
mesh.  The rule remains fixed throughout a trajectory.

The elimination must now be performed in the \emph{weighted sampled}
residual space.  At the predictor \eqref{eq:adaptive-predictor}, define
\begin{equation}
\mathbf r_{\xi,*}=\mathbf S_\xi\mathbf r_*,\qquad
\mathbf J_{\xi,*}=\mathbf S_\xi\mathbf J_*,\qquad
\mathbf A_\xi=\mathbf J_{\xi,*}\mathbf V.
\label{eq:adaptive-weighted-operators}
\end{equation}
If $\mathbf A_\xi$ has full column rank, take its thin QR factorization
$\mathbf A_\xi=\mathbf Q_\xi\mathbf R_\xi$ and form
\begin{equation}
\begin{aligned}
\mathbf P_\xi&=\mathbf I_{N_\xi}-\mathbf Q_\xi\mathbf Q_\xi^T,\\
\mathbf C_\xi&=\mathbf P_\xi\mathbf J_{\xi,*}\overline{\mathbf V},\\
\mathbf H_\xi&=\mathbf C_\xi^T\mathbf C_\xi,\qquad
\mathbf g_\xi=\mathbf C_\xi^T\mathbf P_\xi\mathbf r_{\xi,*}.
\end{aligned}
\label{eq:adaptive-sampled-schur}
\end{equation}
The implementation checks primary rank and rejects a rank-deficient action
rather than adding arbitrary QR directions.  The reported computations
pass that check and attain the requested correction ranks.

The secondary patterns are an orthonormal basis of
\begin{equation}
\mathcal K_r(\mathbf H_\xi,\mathbf g_\xi)
=\operatorname{span}\{\mathbf g_\xi,\mathbf H_\xi\mathbf g_\xi,
\ldots,\mathbf H_\xi^{r-1}\mathbf g_\xi\},\qquad
\mathbf B_{r,\xi}^T\mathbf B_{r,\xi}=\mathbf I_{r_m}.
\label{eq:adaptive-sampled-krylov}
\end{equation}
Here $r_m\leq r$ allows numerical breakdown.  No dense secondary Hessian is
required: the implementation applies the sampled Jacobian, its adjoint, and
$\mathbf P_\xi$ to form the Krylov actions.  Once the basis is frozen, the
Gauss--Newton increment solves
\begin{equation}
\min_{\Delta\mathbf x\in\mathbb R^{n+r_m}}
\norm{\mathbf S_\xi\mathbf r+
\mathbf S_\xi\mathbf J
[\mathbf V\quad\overline{\mathbf V}\mathbf B_{r,\xi}]
\Delta\mathbf x}_2^2,
\qquad \mathbf x=\begin{bmatrix}\q\\\mathbf a\end{bmatrix}.
\label{eq:adaptive-sampled-gn}
\end{equation}
Thus both direction selection and the nonlinear correction solve the same
sampled problem.  Constructing directions from unweighted residual rows,
then inserting them into a weighted solve, would be a different method.
Likewise, a full-residual $\mathbf B_r$ is not simply reused as
$\mathbf B_{r,\xi}$.

Global Euclidean orthogonality of the trial basis still gives
\eqref{eq:adaptive-tangent}; restricting its rows does not give an identity
weighted Gram matrix.  The fitted rule must represent this geometry and the
residual actions sufficiently well.  With unit weights and every cell
retained, \eqref{eq:adaptive-sampled-schur} reduces to the full-residual
construction.  For a reduced rule, the nested linearized residual bound
holds for the \emph{sampled} objective, not automatically for the full
residual or HDM state error.

\subsection{Positive moment fitting for an adaptive trial space}
\label{sec:adaptive-positive-rule}
An ordinary closure ECM rule is fitted to a prescribed tangent.  Here the
secondary tangent changes with the online residual, so fitting only the ten
primary projected contributions would leave the new actions uncontrolled.
We therefore use an additional positive moment fit for the adaptive model.
At an offline anchor $s$, set
$\mathbf Z_s=\mathbf J_s\mathbf V_{\mathrm{tot}}$ and
$\mathbf Y_s=\mathbf J_s[\mathbf V\quad\overline{\mathbf V}\mathbf B_{3,s}]$,
with the full-residual correction directions at that anchor.
For cell $e$, let $\mathbf Z_{s,e}$, $\mathbf Y_{s,e}$, and
$\mathbf r_{s,e}$ contain its two field rows.  The dynamic cell moments are
\begin{equation}
\mathbf h_{s,e}=\left[
\mathbf Z_{s,e}^T\mathbf r_{s,e};\quad
\operatorname{svec}(\mathbf Y_{s,e}^T\mathbf Y_{s,e});\quad
\norm{\mathbf r_{s,e}}_2^2\right].
\label{eq:adaptive-cell-moments}
\end{equation}
The operator $\operatorname{svec}$ stores the upper triangle, multiplying
its off-diagonal entries by $\sqrt{2}$, so its Euclidean norm equals the
Frobenius norm of the symmetric matrix.  Summing these cell contributions
recovers the complete coordinate gradient, the adaptive normal matrix, and
the residual energy.  A shared mass block additionally fits
\begin{equation}
\sum_e\xi_e\mathbf V_{\mathrm{tot},e}^T
\mathbf V_{\mathrm{tot},e}\approx\mathbf I_{151}.
\label{eq:adaptive-mass-moment}
\end{equation}
This is a cubature consistency condition in the standard Euclidean basis,
not a change of POD metric.

The implementation first draws a candidate support using
\begin{equation}
p_e=0.9\frac{\ell_e}{\sum_j\ell_j}+\frac{0.1}{N_e},\qquad
\ell_e=\norm{\mathbf V_{\mathrm{tot},e}}_F^2.
\label{eq:adaptive-support-probability}
\end{equation}
Independent draws with replacement give counts $c_e$ and initial weights
$\xi_e^{(0)}=c_e/(L p_e)$.  The draw count $L$ is neither an SVD tolerance
nor the number of positive fitted cells.  Duplicate draws produce a smaller
support.  On that support, nonnegative multipliers $\mathbf x$ are fitted by
\begin{equation}
\min_{\mathbf x\geq\mathbf0}
\frac12\norm{\mathbf A_{\mathrm{mom}}\mathbf x-
\mathbf b_{\mathrm{mom}}}_2^2,\qquad
\xi_e=\xi_e^{(0)}x_e.
\label{eq:adaptive-positive-fit}
\end{equation}
The matrix stacks the scaled dynamic and complete mass moments, and the
target stacks their full-mesh sums.  This convex moment fit is distinct from
the SVD-compressed ECM selection in Section~\ref{sec:nonlinear-ecm}.
Its numerical quality is checked on held-out parameters before deployment;
optimizer termination alone is not taken as proof of cubature accuracy.

\section{Benchmark and HPROM Protocol}
\label{sec:benchmark}
\subsection{Burgers problem and fixed Euclidean basis}
We use the two-dimensional parametric inviscid Burgers benchmark of
\cite{barnett2023neural},
\begin{equation}
\frac{\partial\mathbf U}{\partial t}+
\frac{\partial\mathbf F(\mathbf U)}{\partial x}+
\frac{\partial\mathbf G(\mathbf U)}{\partial y}=\mathbf S(x;\mub),\qquad
\mathbf U=\begin{bmatrix}u_x\\u_y\end{bmatrix},
\end{equation}
where
\begin{equation}
\mathbf F(\mathbf U)=\begin{bmatrix}\tfrac12u_x^2\\\tfrac12u_xu_y\end{bmatrix},
\quad\mathbf G(\mathbf U)=\begin{bmatrix}\tfrac12u_xu_y\\\tfrac12u_y^2\end{bmatrix},
\quad\mathbf S(x;\mub)=\begin{bmatrix}0.02\exp(\mu_2x)\\0\end{bmatrix}.
\end{equation}
The domain is $[0,100]^2$, $T_f=25$, and the original parameter box is
$\mathcal D=[4.25,5.50]\times[0.015,0.03]$.
The HDM uses a cell-centered finite-volume Godunov discretization on a
$250\times250$ grid: $N_e=62{,}500$ cells and $N=125{,}000$ unknowns.
Backward Euler uses 500 uniform steps with $\Delta t=0.05$.
Nine HDM trajectories on the $3\times3$ grid
$\{4.25,4.875,5.50\}\times\{0.015,0.0225,0.03\}$ define the centered
Euclidean POD basis.  Its retained 151 modes capture $99.9900\%$ of the
snapshot energy.  The basis and mean state are fixed in every reported
campaign; enrichment does not recompute them.

\subsection{Training targets, validation, and reporting parameters}
The main campaigns train on complete trajectories generated by the
151-coordinate linear HPROM of Section~\ref{sec:projection-framework},
not on orthogonal projections of HDM snapshots.  Its positive rule is
constructed once from the baseline data and frozen thereafter.  The same
linear-HPROM teacher generates baseline and enriched labels for every learned
family, including the direct non-intrusive maps.  This is an experimental
control linking their labels to a reduced dynamical solve, not a claim that
they become intrusive models.

Two complete linear-HPROM trajectories are reserved for regression validation,
\begin{equation}
\mub^{\mathrm{val},1}=(4.5625,0.02625),\qquad
\mub^{\mathrm{val},2}=(5.1875,0.01875).
\label{eq:validation-points}
\end{equation}
Holding out complete parameters rather than randomly selected time samples
checks parameter generalization of the regression.  These two trajectories
are never added to the training loss, but are used for checkpoint selection
and adaptive-rule validation.  They are therefore validation data, not an
independent test set.  The reporting parameters are
\begin{equation}
\begin{aligned}
\mub^{(v)}&=(4.875,0.0225),&\mub^{(1)}&=(4.560,0.0190),\\
\mub^{(2)}&=(5.190,0.0260),&\mub^{(3)}&=(4.000,0.0330).
\end{aligned}
\label{eq:evaluation-points}
\end{equation}
Verification $\mub^{(v)}$ belongs to the baseline grid and is not an unseen
training parameter.  The two off-grid reporting parameters are in-domain;
$\mub^{(3)}$ is outside the original box.  None of the reporting errors
is used to select adaptive cubature support or weights.  Verification
nevertheless participates in offline training and moment construction
through its membership in the nine-point grid.

\subsection{Nested enrichment and unchanged regression families}
A fixed seed--42 Latin-hypercube design supplies nested interior and margin
locations.  The margin is the union of four strips in a rectangle enlarged
by $25\%$ of each original side length.  The retained budgets are
$9$, $9+8$, $9+12$, and $9+18$, with equal counts of added interior and
margin points.  The larger 36-point candidate design fixes the ordering
only; a $9+36$ campaign is not part of the reported Euclidean study.
The largest HPROM data set contains all smaller sets, and the repeated
trajectories are exact copies, not newly generated labels.  The two
validation parameters remain unchanged.

\begin{table}[H]
\centering
\caption{Nested linear-HPROM coefficient data.  Totals count complete
trajectories; the two validation trajectories are excluded from training.}
\label{tab:euclidean-enrichment-dataset}
\input{tables/euclidean_hprom/datasets.tex}
\end{table}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{Figures/euclidean_hprom/sampling.pdf}
\caption{HPROM training layouts in the order baseline, $9+8$, $9+12$,
$9+18$.  Black circles mark baseline parameters, coloured points the added
interior and margin parameters, diamonds the two held-out linear-HPROM
regression-validation parameters, and stars the four reporting parameters.
The original parameter box is outlined.  Margin sampling does not guarantee
corner coverage: the $9+12$ subset adds a location near
$(3.980,0.03173)$ in the upper-left region close to $\mub^{(3)}$.
Validation points concern regression and rule auditing, not HDM test data.}
\label{fig:parameter-space}
\end{figure}

The training sets contain 4509, 8517, 10521, and 13527 parameter--time samples,
including their initial states; validation contains 1002 samples.
Cases~1--3 retain $n=10$, and the intrusive autoencoder and POD--DL--ROM
retain $n_z=10$.  Architectures, activations, and nominal optimization
settings are unchanged across enrichment budgets; each family is retrained
and its checkpoint selected using the same validation parameters.
Cases~1--3 and the intrusive autoencoder use coordinate mean-squared error,
with scaling embedded in their networks.  POD--DL--ROM combines prediction,
latent consistency, and reconstruction losses with respective weights
$1$, $0.03$, and $0.01$.  None uses a sensitivity-aware POD metric.
Table~\ref{tab:training} records the baseline
regression accuracy and parameter counts.  Its errors refer to each family's
complete output coordinates: the secondary block for Cases~1 and~3 and the
complete vector for the master map, autoencoder, and POD--DL--ROM.

\begin{table}[H]
\centering
\caption{Baseline regression architectures and relative Frobenius errors
(\%) on training and held-out parameter-validation coordinates.
The Case--2 master map also supplies POD--NN--ROM and Case~2+$\mathbf B_3$;
it is trained once, not separately for these deployments.}
\label{tab:training}
\resizebox{\textwidth}{!}{\input{tables/euclidean_hprom/training_baseline.tex}}
\end{table}

The controlled parameter--time master map predicts the entire vector,
\begin{equation}
\mathcal G_q:(\mub,t)\longmapsto\widehat{\q}_N\in\mathbb R^{151},\qquad
\mathcal M(\mub,t)=\mathbf P_{>10}\mathcal G_q(\mub,t).
\label{eq:master-map}
\end{equation}
POD--NN--ROM uses $\mathcal G_q$ directly.  Case~2 injects only its last
141 entries and determines the first ten by a sampled residual solve.
Case~2+$\mathbf B_3$ retains that same injected tail and solves three extra
amplitudes.  Sharing the map isolates deployment from regression quality.

\begin{figure}[H]
\centering
\fbox{\begin{minipage}{0.94\textwidth}
\centering
\textbf{One parameter--time prediction, three deployments}\\[0.5em]
$\mathcal G_q(\mub,t)=[\widehat{\q};\widehat{\qbar}]
\in\mathbb R^{151}$, with blocks of sizes 10 and 141.\\[0.5em]
\begin{tabular}{l|c|c}
Deployment & Complete output $\q_N$ & Residual-solved unknowns\\
\hline
POD--NN--ROM & $[\widehat{\q};\widehat{\qbar}]$ & none\\[0.3em]
HPROM--ANN Case 2 & $[\q;\widehat{\qbar}]$ & $\q\in\mathbb R^{10}$\\[0.3em]
Case 2+$\mathbf B_3$ & $[\q;\widehat{\qbar}+\mathbf B_3\mathbf a]$
& $(\q,\mathbf a)\in\mathbb R^{13}$
\end{tabular}\\[0.5em]
$\mathbf B_3\in\mathbb R^{141\times3}$ contains three residual-selected
patterns; each amplitude can change all 141 secondary coordinates.
\end{minipage}}
\caption{Coordinate-level interpretation of the controlled comparison.
The same trained network is used in all three rows.  Only deployment changes;
the adaptive patterns are constructed online from the computed predictor,
not from reference trajectories at the reporting parameters.}
\label{fig:shared-map-deployments}
\end{figure}

\subsection{Cubature protocol and initialization}
\label{sec:hprom-protocol}
The conventional nonlinear ECM rules use selected HDM states from the nine
baseline parameters, fitted onto the corresponding trained trial map as in
\eqref{eq:nonlinear-manifold-fit}.  The state-sampling fraction is $2\%$,
with seed 42 and coverage of all nine parameters.  This fraction selects
training states; it is not a residual tolerance.  The contribution SVD uses
a relative singular-value cutoff of $10^{-8}$.  A separate rule is built
for every trained closure or decoder at every enrichment budget.  Equal
cardinalities in Table~\ref{tab:hprom-cubature} do not mean equal weights
or equal sampled optimality problems.  The linear HPROM rule alone is frozen.

For B3 moment fitting, ten time indices
$\{1,2,5,10,25,50,100,200,350,500\}$ are retained at each of the nine
baseline linear-HPROM parameters.  At an offline anchor the predecessor and
primary predictor are taken from its stored teacher trajectory, and the tail
from the corresponding trained master map.  This use of teacher states is
\emph{offline only}.  Deployment uses the computed predecessor, not a future
HDM or linear-HPROM state.  The enriched B3 rules change with their master
maps, but moment fitting still uses only these nine baseline trajectories;
it does not generate additional training simulations.

All four main B3 rules use $L=4096$ candidate draws with seed 418.
The three dynamic moment blocks are separately normalized before fitting;
the complete mass block is scaled to have a comparable aggregate weight.
The positive fit uses exact objective gradients with at most 1500
L-BFGS-B iterations.  Rule acceptance uses seven anchors at each of the two
held-out validation parameters.  For the complete coordinate Jacobian action
$\mathbf Z=\mathbf J_*\mathbf V_{\mathrm{tot}}$, the generalized eigenvalues
of $(\mathbf Z^T\mathbf D_\xi\mathbf Z,\mathbf Z^T\mathbf Z)$ must lie
in $[0.8,1.2]$.  This checks preservation of Jacobian-action geometry, not
only the mass moment in \eqref{eq:adaptive-mass-moment}.
The relative gradient discrepancy
$\norm{\mathbf Z^T\mathbf D_\xi\mathbf r_*-\mathbf Z^T\mathbf r_*}_2/
\norm{\mathbf Z^T\mathbf r_*}_2$ must not exceed $0.05$.
Finally, full and sampled B3 linearized updates are computed using their
respective direction spaces.  The ratio of their residual norms, both
evaluated in the full residual, must not exceed $1.05$.
Its denominator is the full-residual B3 update at 13 unknowns, not the
unrestricted 151-coordinate optimum.  These are empirical operator audits,
not a uniform nonlinear error bound.
Table~\ref{tab:b3-validation} records their extrema.

\begin{table}[H]
\centering
\caption{Validation-only acceptance of the four 4096-draw B3 rules.
Eigenvalues compare sampled and full complete-coordinate Jacobian Grams.
Gradient error is a ratio, not a percentage.  ``LS ratio'' evaluates the
sampled B3 linearized update in the full residual norm and divides by the
full-residual B3 linearized value.  Reporting points do not enter acceptance.}
\label{tab:b3-validation}
\resizebox{\textwidth}{!}{\input{tables/euclidean_hprom/b3_validation.tex}}
\end{table}

\begin{table}[H]
\centering
\caption{Positive cubature cell counts at each data budget.
B3 has a dedicated moment-fitted rule; the other nonlinear models use
map-specific ECM.  State stencil sizes can exceed retained residual-cell
counts because of neighbour dependencies.}
\label{tab:hprom-cubature}
\resizebox{\textwidth}{!}{\input{tables/euclidean_hprom/cubature.tex}}
\end{table}

The adaptive computations use the known initial condition represented in the
complete linear space,
\begin{equation}
\q_N^0=\mathbf V_{\mathrm{tot}}^T(\uvec_0-\uvec_{\mathrm{ref}}),\qquad
\uvec_r^0=\uvec_{\mathrm{ref}}+\mathbf V_{\mathrm{tot}}\q_N^0.
\label{eq:adaptive-common-initial}
\end{equation}
This predecessor need not belong to the learned manifold at $t=0$.
The conventional learned HPROM campaign instead reconstructs its initial
state on the corresponding trained closure or decoder manifold;
direct maps predict their initial
coordinates.  These existing outputs are retained without reinterpretation.
Consequently the main B3-versus-Case--2 comparison includes an initialization
difference as well as a trial-space change.  The matched-initialization,
equal-dimensional PROM control in Appendix~\ref{sec:adaptive-results}
addresses direction selection separately.  An equivalent matched HPROM
rank/control study has not been run and is not claimed here.
All requested B3 ranks were three throughout the 500-step reporting runs.
Its nonlinear solver permits 20 Gauss--Newton iterations, with relative
residual cutoff $10^{-5}$ and relative residual-change threshold $10^{-2}$.

\subsection{Errors and scope of online measurements}
For every reporting parameter we reconstruct the complete state and use
\begin{align}
e_u(\mub)=100\frac{\norm{\uvec_{\mathrm{HDM}}-\uvec_{\mathrm{ROM}}}_F}
{\norm{\uvec_{\mathrm{HDM}}}_F},\label{eq:state-error}\\
e_q(\mub)=100\frac{\norm{\q_N^{\mathrm{lin,H}}-\widehat{\q}_N}_F}
{\norm{\q_N^{\mathrm{lin,H}}}_F}.
\label{eq:coefficient-error}
\end{align}
The histories contain all 501 saved states.  The coefficient reference is
always the frozen full-coordinate \emph{linear HPROM}, even for non-intrusive
maps.  The mean averages $\mub^{(v)},\mub^{(1)},\mub^{(2)}$ only;
extrapolation is reported separately.  This reference is not a strict
HDM-error lower bound: other trajectories can have slightly smaller state
error while differing from its coefficient history.
Per-coordinate absolute errors are
$\norm{\widehat q_{N,i}-q_{N,i}^{\mathrm{lin,H}}}_2$ over time; relative
plots divide by $\norm{q_{N,i}^{\mathrm{lin,H}}}_2$ and multiply by 100.
Their logarithmic limits are shared across all four data budgets and include
all positive plotted values.  Pointwise coefficient heat maps use the same
reference and the same complete-history denominator for each coordinate,
not division by an instantaneous coefficient that might cross zero.

Baseline timings are measured on the same Sherlock node.  The fresh HDM and
linear HPROM use 24 numerical threads; all learned models use one CPU
numerical thread.  This is the declared execution protocol, not an
identical-thread scaling experiment.  Intrusive solve times exclude model
loading, cubature fitting, plotting, and file I/O, but include B3 construction
when present.  One trajectory measurement per reporting parameter is retained,
without an estimate of timing variability.  Direct maps use ten repeated
\emph{loaded-network coefficient-history predictions}; they exclude
high-dimensional reconstruction and loading.  Their HDM/time ratios are thus
coefficient-inference ratios, not end-to-end simulator speedups.
No PROM timing is reported.  Enrichment jobs need not share this timing node,
so their times are not mixed into the baseline speedup comparison.

\section{Baseline HPROM Results}
\label{sec:hprom-results}
\subsection{State accuracy and solution profiles}
Table~\ref{tab:online-state} compares the baseline models in a fixed order:
linear reference, Cases~1 and~2, corrected Case~2, Case~3, intrusive decoder,
and the two non-intrusive maps.  This order is retained in every subsequent
accuracy table and comparison figure.  Horizontal rules separate the
residual-solving models from the direct regressors.

\begin{table}[H]
\centering
\caption{Baseline HPROM state-history errors $e_u$ (\%).
The mean averages the three in-domain points; the last column is
extrapolatory.  B3 uses the known full-space initial representation, unlike
the conventional learned runners; see Section~\ref{sec:hprom-protocol}.}
\label{tab:online-state}
\resizebox{\textwidth}{!}{\input{tables/euclidean_hprom/baseline_state.tex}}
\end{table}

The linear HPROM has mean state error $0.441\%$.  Cases~1 and~3 and the
intrusive decoder give $0.553\%$, $0.494\%$, and $0.583\%$, respectively.
Case~2 gives $1.620\%$, close to the $1.701\%$ direct master-map error.
Solving the ten primary coordinates therefore improves the mean only modestly
when the tail remains prescribed.  It does not improve every point:
at $\mub^{(1)}$ its error exceeds that of POD--NN--ROM.
At extrapolation, however, Case~2 gives $2.801\%$ rather than the direct
map's $5.102\%$.  Residual minimization supplies useful correction without
making the learned secondary block accurate by itself.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth,height=.72\textheight,keepaspectratio]{Figures/euclidean_hprom/baseline_solutions.pdf}
\caption{Baseline solution cut-plane overlays.  Each row is one reporting
parameter; columns show $u_x(x,y_{\mathrm{mid}})$ and
$u_x(x_{\mathrm{mid}},y)$.  Increasing opacity denotes $t=5,15,25$;
all model curves are solid.  All panels and enrichment counterparts share
state ordinate limits.  Overlapping profiles should be read with the
space--time errors, not as evidence of identical trajectories.}
\label{fig:hprom-baseline-solutions}
\end{figure}

\subsection{Complete coefficient recovery}
\begin{table}[H]
\centering
\caption{Baseline complete 151-coordinate trajectory errors $e_q$ (\%)
against the frozen linear HPROM.  Parameter and model ordering match
Table~\ref{tab:online-state}.  The direct maps have the same teacher but
perform no sampled-residual solve.}
\label{tab:online-coefficients}
\resizebox{\textwidth}{!}{\input{tables/euclidean_hprom/baseline_coefficient.tex}}
\end{table}

The mean coefficient errors of Case~2 and POD--NN--ROM are $3.667\%$ and
$3.859\%$, compared with $0.774\%$ and $0.575\%$ for Cases~1 and~3.
The difference therefore appears in the reduced trajectories themselves,
not only in state reconstruction.  Figure~\ref{fig:coefficient-curves}
resolves it by coordinate.  Large relative errors in high-index coefficients
can coexist with a small total state error because those coefficient
histories have smaller absolute magnitudes.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{Figures/euclidean_hprom/baseline_coefficients.pdf}
\caption{Baseline per-coordinate trajectory errors against the linear HPROM:
absolute errors above, relative errors (\%) below.  Columns follow
$\mub^{(v)},\mub^{(1)},\mub^{(2)},\mub^{(3)}$.
All model curves are solid; the dotted vertical line marks coordinate ten.
Each row uses common data-derived logarithmic limits across all baseline
and enriched plots.  The zero-error linear reference is omitted.}
\label{fig:coefficient-curves}
\end{figure}

\section{Secondary Recovery and Hyperreduced Online Work}
\label{sec:hprom-b3-results}
\subsection{What three correction amplitudes recover}
Case~2+$\mathbf B_3$ gives mean state error $0.455\%$ and extrapolation
error $0.845\%$, close to the frozen linear HPROM's $0.441\%$ and
$0.850\%$.  Its mean coefficient error is $0.243\%$, and the extrapolation
coefficient error is $0.336\%$.  The unchanged baseline master map supplies
the initial tail prediction.  Recovery uses neither extra ANN training
parameters nor online access to future reference states.
With $n=10$ and $\bar n=141$, each $141\times3$ matrix contains three
patterns over the \emph{entire} secondary block; the solve has 13 independent
amplitudes while the reconstructed $\q_N$ still has 151 entries.

These results establish the performance of the tested hyperreduced algorithm,
not a clean estimate of the correction's isolated causal effect, because the
main conventional initial states differ.  Appendix~\ref{sec:adaptive-results}
provides the corresponding full-residual matched experiment: at 13 unknowns,
three adaptive secondary patterns give mean state error $0.447\%$, whereas
freeing the next three POD coordinates gives $1.513\%$.  That diagnostic
supports the mechanism, but its numbers are PROM results and are not
substituted into the HPROM tables.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{Figures/euclidean_hprom/b3_histories.pdf}
\caption{Instantaneous HDM-relative state errors.  Top: baseline linear
HPROM, Cases~1--3, and corrected Case~2.  Bottom: corrected Case~2 at all
four data budgets.  Columns follow the four reporting parameters; all
panels share ordinate limits and all model curves are solid.  Instantaneous
norm ratios differ from the space--time Frobenius ratios in the tables.}
\label{fig:hprom-b3-histories}
\end{figure}

\subsection{Baseline timings and measured cost}
\begin{table}[H]
\centering
\caption{Baseline online measurements on one Sherlock node.  Times average
only the three in-domain parameters.  The fresh HDM reference is
$736.729$ s using 24 threads.  HDM/time divides this fixed HDM time by model time;
linear HPROM uses 24 threads and learned models one.
$\dagger$: direct-map time is loaded-network coefficient inference only,
excluding full-state reconstruction, so its ratio is not an end-to-end
simulator speedup.  Intrusive rows use one measurement per point.}
\label{tab:hprom-timing}
\resizebox{\textwidth}{!}{\input{tables/euclidean_hprom/timing_baseline.tex}}
\end{table}

The 4096-draw B3 implementation costs $19.597$ s on average, including
its online direction construction.  Its smaller nonlinear tangent alone
does not make it cheaper than Case~3 ($18.856$ s): the correction construction
acts on all secondary modes and its retained cell count is larger.
The nonlinear ECM models retain 901 cells, whereas this B3 rule retains
3532.  Accuracy and total online work must therefore be evaluated together,
not inferred from the 13 versus ten online unknowns.

\subsection{Reducing support without changing the correction model}
A separate baseline-only budget study keeps $r=3$ and the baseline network
fixed.  Predictor residual/Jacobian evaluations are reused in both compared
variants, without changing the sampled equations.  Validation then selects a
smaller positive support before any reporting-point evaluation.  The
2048-draw rule retains 1506 positive cells.  This choice passes the operator
audits and an additional trajectory check: at each validation parameter
its complete coefficient error may be at most $1.05$ times that of the
4096-draw parent rule.  These are validation comparisons, not reporting-point
acceptance criteria.  A local
1024-draw stress test fails acceptance and is not deployed at reporting
points.  Thus support reduction is controlled by validation rather than by
choosing an arbitrary cell count after observing test errors.

\begin{table}[H]
\centering
\caption{Separate baseline B3 support comparison with predictor reuse in
both variants, one CPU thread, and one trajectory measurement per point.
The HDM reference is the same $736.729$ s.  Draw count is not positive
cell count.  These timings must not be interchanged with the unreused
4096-draw campaign row in Table~\ref{tab:hprom-timing}.  The smaller rule
is not the rule used in the enrichment study.}
\label{tab:hprom-b3-budget}
\resizebox{\textwidth}{!}{\input{tables/euclidean_hprom/b3_budget.tex}}
\end{table}

Reducing positive support from 3532 to 1506 cells decreases the mean measured
time from $17.477$ s to $8.433$ s, a $51.7\%$ reduction, with nearly
unchanged state error.  The selected variant is cheaper than the conventional
Cases~1 and~3 under this execution protocol.  Its $87.36$ HDM/time ratio
is a single-measurement observation, not a timing-confidence estimate or a
claim of optimal support.  The additional fitting and validation are offline
costs, not included in the solve time.

\section{Effect of HPROM Coefficient-Data Enrichment}
\label{sec:hprom-enrichment}
\subsection{Regression accuracy at fixed model families}
Enrichment changes the linear-HPROM coefficient labels supplied to regression,
not the POD basis or the held-out validation and reporting parameters.
Each trained intrusive trial map receives a newly built positive rule.
The comparison consequently assesses the complete enriched HPROM workflow;
it is not a cubature-frozen ablation of the regression alone.  B3 uses the
4096-draw fit at every budget, with the same nine baseline moment-anchor
trajectories and the newly trained master map.

\begin{table}[H]
\centering
\caption{Relative coordinate regression errors (\%) at the four HPROM data
budgets.  Each vertical rule separates complete training/validation pairs.
The master-map row is shared by conventional Case~2, B3, and POD--NN--ROM.
These family-specific coordinate errors are not state-history errors.}
\label{tab:hprom-enrichment-training}
\resizebox{\textwidth}{!}{\input{tables/euclidean_hprom/training_enrichment.tex}}
\end{table}

\subsection{In-domain improvement and extrapolation are different tests}
\begin{table}[H]
\centering
\caption{HPROM state-history errors $e_u$ (\%) across nested data budgets.
Each budget has the in-domain mean followed by extrapolation.
Vertical rules separate budgets; the horizontal rule separates intrusive
residual solves from direct maps.  The linear HPROM is unchanged.}
\label{tab:hprom-enrichment-state}
\resizebox{\textwidth}{!}{\input{tables/euclidean_hprom/enrichment_state.tex}}
\end{table}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{Figures/euclidean_hprom/enrichment_state.pdf}
\caption{Effect of the nested HPROM coefficient-data ladder on state errors.
Left: mean of the three in-domain parameters.  Right: extrapolation.
Both panels use exactly the same state-error limits, including the largest
plotted bar.  Black horizontal lines denote the fixed linear HPROM;
dashed vertical lines separate intrusive models from direct maps.
B3 uses the 4096-draw rules at all four budgets.}
\label{fig:hprom-enrichment-state}
\end{figure}

Case~2's in-domain mean decreases from $1.620\%$ to $0.719\%$,
$0.573\%$, and $0.488\%$ as 8, 12, and 18 trajectories are added.
The corresponding direct master-map errors are $1.701\%$, $0.754\%$,
$0.585\%$, and $0.490\%$.  Hence conventional Case~2 behaves as a
partially residual-corrected direct map: its mean advantage is real but
modest, and shrinks as regression improves.  At $9+18$ it is near, but
not identical to, the $0.441\%$ linear reference.
Cases~1 and~3 and the intrusive decoder already approach that reference
at $9+8$; their largest-budget means are $0.438\%$, $0.439\%$,
and $0.441\%$.  Small values below the linear reference do not violate an
optimality bound, because the minimized residual is not the HDM state error.

Extrapolation is not monotone in the number of labels.  From baseline to
$9+8$, Case~2 worsens from $2.801\%$ to $3.621\%$ and POD--NN--ROM
from $5.102\%$ to $10.836\%$, despite improved in-domain regression.
At $9+12$ these errors become $1.471\%$ and $3.296\%$; at $9+18$,
$0.886\%$ and $0.982\%$.  The extra $9+12$ point near the upper-left
corner supplies coverage absent from the smaller subset
(Fig.~\ref{fig:parameter-space}).  The trend is consistent with a coverage
explanation, but does not prove that this single point causes the recovery:
other locations, retraining, checkpoint selection, and rule fitting also
change.  Adding margin points is not an extrapolation guarantee.

The adaptive means remain $0.455\%$, $0.441\%$, $0.443\%$, and
$0.443\%$ across the four budgets, with extrapolation errors
$0.845\%$, $0.871\%$, $0.849\%$, and $0.849\%$.
Its primary benefit is recovery \emph{before} extensive regression enrichment.
This does not make new data unnecessary: enrichment further reduces its
coefficient discrepancy and can improve other deployment families without
an adaptive online construction.

\subsection{Coordinate consistency and enriched solution fields}
\begin{table}[H]
\centering
\caption{Complete coefficient-history errors $e_q$ (\%) against the frozen
linear HPROM, in the same budget and model order as
Table~\ref{tab:hprom-enrichment-state}.}
\label{tab:hprom-enrichment-coefficients}
\resizebox{\textwidth}{!}{\input{tables/euclidean_hprom/enrichment_coefficient.tex}}
\end{table}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{Figures/euclidean_hprom/enrichment_coefficient.pdf}
\caption{Coefficient-history counterpart of
Fig.~\ref{fig:hprom-enrichment-state}.  Both panels share coefficient-error
limits; all errors use the same fixed linear-HPROM reference.  Dashed
vertical separators distinguish residual-solving and direct models.}
\label{fig:hprom-enrichment-coefficients}
\end{figure}

B3's mean complete-coordinate discrepancy decreases from $0.243\%$ to
$0.063\%$, $0.039\%$, and $0.034\%$ across the ladder.  Thus its nearly
constant HDM state errors do not mean that enrichment has no effect: the
trajectory approaches the fixed teacher more closely while its state error
is already dominated by that teacher's discrepancy from the HDM.
Conventional Case~2 gives means $3.667\%$, $1.434\%$, $0.920\%$,
and $0.607\%$; the direct master-map means remain close at
$3.859\%$, $1.514\%$, $0.944\%$, and $0.607\%$.
At extrapolation their $9+8$ coefficient discrepancies grow to
$8.278\%$ and $25.405\%$, whereas B3 remains at $0.443\%$.
This supports an actual recovery of the reduced trajectory, not only
fortuitous cancellation in a total HDM state norm.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth,height=.72\textheight,keepaspectratio]{Figures/euclidean_hprom/lhs18_solutions.pdf}
\caption{$9+18$ HPROM solution overlays, with exactly the same model order,
cut planes, times, and ordinate limits as
Fig.~\ref{fig:hprom-baseline-solutions}.  Initializations follow the campaign
protocol in Section~\ref{sec:hprom-protocol}.}
\label{fig:hprom-enriched-solutions}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{Figures/euclidean_hprom/lhs18_coefficients.pdf}
\caption{$9+18$ per-coordinate HPROM trajectory errors, with the same
reference, model colours, coordinate split, and shared logarithmic limits as
Fig.~\ref{fig:coefficient-curves}.  All models, including both non-intrusive
maps and B3, are shown.}
\label{fig:hprom-enriched-coefficients}
\end{figure}

Appendix~\ref{app:hprom-details} gives the pointwise tables and the matching
solution and coefficient figures for the intermediate budgets.
The data budgets remain in this report rather than selecting a preferred
subset solely from the reporting errors.  The study is limited to one
Burgers discretization, two validation parameters, four reporting parameters,
and one training realization per family and budget.

\section{Conclusions}
\label{sec:conclusions}
The main comparison now uses HPROM-generated training trajectories and
model-specific positive cubature in a fixed Euclidean POD basis.
Conventional parameter--time Case~2 improves the shared direct map modestly
in mean accuracy but remains exposed to its prescribed secondary errors.
State-conditioned and hybrid closures are more accurate with sparse labels
for the tested Burgers problem.

A small residual-adaptive secondary space provides a different response to
limited regression accuracy.  Its algebra follows from primary elimination
in the current weighted sampled linearized residual problem; its directions
remain fixed during each nonlinear solve.  With three patterns over 141
secondary coordinates, the tested 13-unknown HPROM is close to the
151-coordinate linear HPROM before enrichment.  The full-residual
matched-initialization, equal-dimensional appendix supports the importance
of direction selection, but does not replace the still-missing matched HPROM
rank study.  No nested-space argument guarantees a nonlinear trajectory's
HDM error.

Enrichment and online correction are complementary, not interchangeable.
The $9+8$ set improves in-domain means yet worsens extrapolation for the
master-map-based uncorrected deployments.  The $9+12$ and $9+18$ sets
recover extrapolation as coverage changes.  B3 stays near the fixed reference
at all tested budgets and becomes increasingly coefficient-consistent.
The separate validated support reduction approximately halves its online
time without changing the network or correction rank.  Timing conclusions
remain specific to the declared thread protocol and single-trajectory
measurements; direct inference ratios exclude full-state reconstruction.
A second transport benchmark, independent validation parameters, repeated
training, and matched HPROM rank/initialization controls are needed before
claiming general robustness or optimality.

\appendix
\section{Full-Residual PROM Direction-Selection Controls}
\label{sec:adaptive-results}
\subsection{Diagnostic protocol}
\label{sec:adaptive-protocol}
These previously completed experiments use full-residual PROM trajectories
and a master ANN trained on nine linear-PROM trajectories, with the two
validation parameters in \eqref{eq:validation-points}.  They are not HPROM
runs, do not use the HPROM-trained master network, and report no timings.
Their purpose is to isolate trial-space orientation before cubature.
All compared trajectories use the known initial representation
\eqref{eq:adaptive-common-initial}, the same full residual, and the same
nominal nonlinear solver settings.  The tested ranks are
\begin{equation}
r\in\{0,1,2,3,5,10\}.
\label{eq:adaptive-tested-ranks}
\end{equation}
The $n=10$ adaptive model is paired at every rank with conventional Case~2
having $d=10+r$ primary coordinates and trial map
\begin{equation}
\boldsymbol\phi_d^{\mathrm{C2}}(\mathbf x;\mub,t)
=\uvec_{\mathrm{ref}}+\mathbf V_{1:d}\mathbf x+
\mathbf V_{d+1:151}\mathbf P_{>d}\mathcal G_q^{\mathrm{PROM}}(\mub,t),
\qquad\mathbf x\in\mathbb R^d.
\label{eq:case2-equal-dimension-control}
\end{equation}
Thus each pair has the same number of unknowns and the same network.
A preliminary validation-only screening had frozen $r=3$ before reporting
parameters were evaluated; the subsequent expanded characterization does
not reselect that rank.  These validation locations also serve network
selection, so the rank sweep is diagnostic rather than an independent
accuracy test.  Only the frozen $r=3$ and its $n=13$ conventional control
were evaluated at all four reporting parameters.

The initial choice $r=3$ was frozen by the preliminary validation screening
described in Appendix~\ref{sec:adaptive-protocol}.  After that selection, we
expanded the validation-only characterization to
$r\in\{0,1,2,3,5,10\}$.  This additional sweep does not reselect the rank and
does not use any reporting parameter.  Every run uses the same master ANN,
$n=10$, the complete residual and Jacobian, and the common initial
representation \eqref{eq:adaptive-common-initial}.  Thus $r=0$ is the
matched uncorrected Case~2 trajectory.  For every $r$, the comparison also
includes the plain Case--2 map \eqref{eq:case2-equal-dimension-control} with
$n=10+r$.  In particular, $\mathbf B_5$ and $\mathbf B_{10}$ are compared
with Case~2 at $n=15$ and $n=20$, respectively.

\begin{table}[H]
\centering
\caption{Validation-only, equal-dimensional full-residual PROM comparison.
All six error columns report percentages for the complete 151-coordinate
trajectory against the corresponding linear PROM.  At each row both models
have $d=10+r$ online unknowns: the adaptive model retains $n=10$ and adds
$r$ residual-selected secondary directions, whereas the control directly
solves the first $10+r$ POD coordinates.  All requested ranks were attained
at every time step.}
\label{tab:adaptive-rank-validation}
\resizebox{\textwidth}{!}{\input{tables/euclidean_prom_only/euclidean_case2_rank_validation.tex}}
\end{table}

\begin{figure}[H]
\centering
\includegraphics[width=0.94\textwidth]{Figures/euclidean_prom_only/euclidean_case2_rank_validation.png}
\caption{Validation-only comparison at equal online dimension
$d=10+r$.  Left: mean complete coefficient-history error against linear PROM.
Right: residual norm at termination of each nonlinear solve, averaged over
time steps and the two validation parameters, then normalized by the common
$r=0$, $n=10$ value.  The star identifies the previously frozen $r=3$ rather
than a rank selected from this expanded sweep.}
\label{fig:adaptive-rank-validation}
\end{figure}

For the residual-adaptive model, the mean coefficient error decreases from
$3.873\%$ at $r=0$ to $1.937\%$, $0.557\%$, and $0.231\%$ at
$r=1,2,3$, respectively.  Increasing the rank to five and ten further
reduces that error to $0.124\%$ and $0.111\%$.  The equal-dimensional
conventional controls do not exhibit this recovery: their means are
$3.809\%$, $3.677\%$, $3.720\%$, $3.643\%$, and $3.542\%$ for
$n=11,12,13,15,20$.  At 13 unknowns, for example, the adaptive and
conventional validation errors are $0.231\%$ and $3.720\%$; at 20 unknowns
they are $0.111\%$ and $3.542\%$.

The residual data give the same qualitative distinction.  The adaptive
mean converged-residual ratio decreases from one to $0.361$ by $r=3$ and
then remains essentially unchanged.  The corresponding conventional ratio
remains above $0.982$ even at $n=20$.  Therefore the observed improvement
cannot be explained by online dimension alone.  For this benchmark, the
Krylov directions in \eqref{eq:adaptive-krylov} capture combinations of the
secondary coordinates that are much more effective for the local LSPG
problem than the next POD modes selected by snapshot energy.

This comparison remains diagnostic rather than an error theorem.  The
nested-space result \eqref{eq:adaptive-nested-bound} concerns a fixed
linearization, whereas the table reports complete nonlinear trajectories.
Moreover, the two parameters used here also served for network validation.
The expanded sweep therefore documents rank dependence and the mechanism of
the recovery, but it neither supplies an independent generalization estimate
nor changes the previously frozen reporting choice $r=3$.  Consequently,
only $\mathbf B_3$ and its $n=13$ equal-dimensional control are evaluated at
the four reporting parameters below.

\subsection{Equal-dimensional control and state accuracy}
We now assess the residual-adaptive construction of
Section~\ref{sec:adaptive-case2} under the dedicated protocol of
Appendix~\ref{sec:adaptive-protocol}.  The notation ``Case~2 + $\mathbf B_3$''
denotes three directions rebuilt at every time step, not a fixed offline
basis.  All learned models in this comparison retain their baseline
training data; enriched HPROMs are examined in Section~\ref{sec:hprom-enrichment}.

\begin{table}[H]
\centering
\caption{Common-initialization full-residual comparison: state-history error
$e_u$ (\%).  Every learned model uses baseline training data.  The mean
averages the three in-domain points; the final column is extrapolatory.
The $n=13$ Case--2 row controls for the 13 online unknowns used by
Case~2+$\mathbf B_3$.  The linear PROM is the reference for the fixed
151-mode basis.}
\label{tab:adaptive-state}
\resizebox{\textwidth}{!}{\input{tables/euclidean_prom_only/euclidean_case2_adaptive_state.tex}}
\end{table}

Table~\ref{tab:adaptive-state} shows a reduction of the mean in-domain state
error from $1.617\%$ for uncorrected Case~2 to $0.447\%$ for the adaptive
variant, a $72.4\%$ reduction.  The latter is close to the linear-PROM
mean of $0.441\%$.  At the extrapolatory parameter the error decreases
from $2.643\%$ to $0.850\%$, also close to the linear reference.
These changes require neither new coefficient training data nor additional
network fitting.  They result from allowing three residual-selected
secondary patterns to be corrected together with the ten primary coordinates.

Merely increasing the primary dimension from $n=10$ to $n=13$ reduces the
mean state error only from $1.617\%$ to $1.513\%$, and the extrapolatory
error from $2.643\%$ to $2.409\%$.  With the same 13 online unknowns,
Case~2+$\mathbf B_3$ gives $0.447\%$ and $0.850\%$, respectively.  The
observed recovery therefore cannot be attributed solely to adding three
degrees of freedom: the residual-selected combinations of the 141 secondary
coordinates are substantially more effective here than freeing POD
coordinates 11--13.

The corrected PROM improves on Cases~1 and~3 in mean error and at
extrapolation, but not at every parameter:
Case~3 has a slightly smaller state error at verification.  Likewise, the
linear PROM is not a strict HDM-error lower bound; at $\mub^{(1)}$ the
corrected trajectory has a slightly smaller HDM error than that reference.

\begin{figure}[H]
\centering
\includegraphics[width=0.96\textwidth]{Figures/euclidean_prom_only/euclidean_case2_adaptive_state_histories.png}
\caption{Instantaneous relative state errors
$100\norm{\uvec_{\mathrm{HDM}}(t)-\uvec_{\mathrm{ROM}}(t)}_2/
\norm{\uvec_{\mathrm{HDM}}(t)}_2$ for the common-initialization comparison.
Panels follow the four reporting parameters and share the same ordinate
range.  All model curves are solid.  These time-resolved errors complement,
rather than replace, the space--time norm in Table~\ref{tab:adaptive-state}.}
\label{fig:adaptive-state-histories}
\end{figure}

\subsection{Recovery of the full coordinate trajectory}
\begin{table}[H]
\centering
\caption{Common-initialization full-residual comparison: complete
151-coordinate trajectory error $e_q$ (\%) against the corresponding
linear PROM.  Means and parameter ordering match Table~\ref{tab:adaptive-state}.}
\label{tab:adaptive-coefficients}
\resizebox{\textwidth}{!}{\input{tables/euclidean_prom_only/euclidean_case2_adaptive_coefficient.tex}}
\end{table}

The mean coefficient error decreases from $3.678\%$ to $0.215\%$
(Table~\ref{tab:adaptive-coefficients}).  At extrapolation it decreases
from $6.104\%$ to $0.441\%$.  The corrected mean is smaller than the
$0.823\%$ and $0.579\%$ of Cases~1 and~3, respectively.
The equal-dimensional $n=13$ control retains a mean coefficient error of
$3.438\%$ and an extrapolatory error of $5.525\%$.
Thus the state-error improvement is accompanied by recovery of the
linear-PROM coordinate history, rather than only by a fortuitous cancellation
in the HDM state norm.  Figure~\ref{fig:adaptive-coefficients} resolves the
errors by coordinate.  Although only three additional amplitudes are solved,
their directions can change all 141 secondary coordinates.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{Figures/euclidean_prom_only/euclidean_case2_adaptive_coefficients.png}
\caption{Common-initialization comparison: per-coordinate absolute
trajectory error (top) and relative trajectory error in percent (bottom)
against the linear PROM.  All panels in each row share logarithmic ordinate
limits computed from the plotted data.  The dotted vertical line marks
the ten primary coordinates; every model curve is solid.  The zero-error
linear-PROM reference is omitted from the logarithmic plots.}
\label{fig:adaptive-coefficients}
\end{figure}

For these four parameters, a small online secondary correction substantially
closes the baseline Case--2 accuracy gap without enriching its training set.
This is not evidence that enrichment is unnecessary: enrichment changes the
offline data, whereas the adaptive construction uses the current residual
and Jacobian during deployment.  Their HPROM interaction is examined in Section~\ref{sec:hprom-enrichment}.
No general state-error guarantee follows from this experiment.

\section{Full-Residual Case--2 Sweep and Perturbation Diagnostic}
\label{sec:prom-results}
The following retained PROM diagnostics motivate secondary correction but
are not evidence about an HPROM perturbation or primary-dimension sweep.
They use the Euclidean basis, full-residual linear-PROM references, and
PROM-trained master map of the original campaign.  Their numbers are kept
separate from the linear-HPROM teacher used in the main text.

\subsection{Case--2 solved-dimension sweep}
\label{sec:case2-sweep}
The shared master map permits an interpolation between a direct parameter--time
regression and the full-coordinate linear PROM.  Table~\ref{tab:case2-sweep}
and Fig.~\ref{fig:case2-sweep-state} show that increasing $n$ reduces state
error, with the sharpest improvement between $n=50$ and $n=100$.  This records
the balance in this controlled Case--2 setting between coordinates supplied by
the ANN and coordinates corrected by the residual.

\begin{table}[H]
\centering
\caption{Case--2 state errors as the number of residual-solved primary
coordinates is varied.  The $n=0$ and $n=151$ endpoints are respectively the
shared-map POD--NN--ROM and the linear PROM.}
\label{tab:case2-sweep}
\resizebox{0.94\textwidth}{!}{\input{tables/euclidean_prom_only/euclidean_prom_case2_n_sweep_state_errors.tex}}
\end{table}

\begin{figure}[H]
\centering
\includegraphics[width=0.78\textwidth]{Figures/euclidean_prom_only/euclidean_prom_case2_n_sweep_state_errors.png}
\caption{Case--2 state error versus the solved primary dimension.  The black
curve is the in-domain mean and the dotted line marks the baseline $n=10$
split.}
\label{fig:case2-sweep-state}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{Figures/euclidean_prom_only/euclidean_prom_case2_n_sweep_coefficient_errors.png}
\caption{Case--2 coefficient trajectories in the shared-master-map sweep.
Top: absolute error against linear PROM.  Bottom: the corresponding relative
error.  The $n=151$ linear-PROM reference has zero coefficient error and is
therefore omitted.}
\label{fig:case2-sweep-coefficients}
\end{figure}

\subsection{Controlled Case--2 tail perturbation}
\label{sec:tail-perturbation}
To test the transfer mechanism independently of a regression magnitude, we
start from the linear-PROM secondary history $\qs^{\mathrm{lin}}$ and add a
perturbation in the direction of the actual master-map secondary error:
\begin{equation}
{\qs}_{\epsilon}=\qs^{\mathrm{lin}}+
\frac{\epsilon}{100}\frac{\widehat{\qs}-\qs^{\mathrm{lin}}}
{\norm{\widehat{\qs}-\qs^{\mathrm{lin}}}_F}
\norm{\qs^{\mathrm{lin}}}_F.
\label{eq:tail-perturbation}
\end{equation}
For each prescribed percentage $\epsilon$, Case~2 solves only the
first ten coordinates.  Thus the diagnostic uses the observed ANN-error
direction but does not conflate its effect with a change of learned model.
Table~\ref{tab:tail-perturbation} and Fig.~\ref{fig:tail-perturbation} show a
monotone transfer from secondary-tail error to the recovered primary block.
The amount of transfer is parameter dependent, so a tail loss alone does not
imply a pointwise primary-coordinate bound.

\clearpage
\begin{table}[H]
\centering
\caption{Primary-coordinate error induced by a controlled secondary-tail
perturbation in Case~2 with $n=10$.  Each entry is relative to the
corresponding linear-PROM primary history.}
\label{tab:tail-perturbation}
\resizebox{0.9\textwidth}{!}{\input{tables/euclidean_prom_only/euclidean_prom_case2_tail_primary_errors.tex}}
\end{table}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{Figures/euclidean_prom_only/euclidean_prom_case2_tail_sensitivity.png}
\caption{Euclidean-POD Case--2 sensitivity to a prescribed secondary-tail
error, restricted to perturbations up to $50\%$.  Left: state error against
the HDM.  Right: error in the ten residual-solved primary coordinates against
linear PROM.}
\label{fig:tail-perturbation}
\end{figure}

\section{Pointwise HPROM Enrichment and Coefficient Histories}
\label{app:hprom-details}
The following panels and tables use only the downloaded HPROM campaigns.
They retain the main-text model ordering and the same frozen linear-HPROM
coefficient reference, shared limits, and initialization conventions.
Heat maps store absolute instantaneous coordinate differences.  Relative
maps divide each row by its complete linear-HPROM history norm and multiply
by 100; they are not instantaneous relative errors.  Colour intensity uses
one common magnitude range per error type across all budgets and models,
with hue identifying the model.  Intensity is logarithmic except for a linear
interval from zero to $10^{-4}$ times the common maximum.  This permits
small correction errors and exact zeros to remain visible without clipping
the extrapolation peaks.  Each row has a numerical colour bar.

\subsection{The \texorpdfstring{$9+8$}{9+8} campaign}
\begin{table}[H]
\centering
\caption{Pointwise $9+8$ HPROM state-history errors $e_u$ (\%).
The mean averages the three in-domain reporting points.}
\label{tab:hprom-lhs8-state}
\resizebox{\textwidth}{!}{\input{tables/euclidean_hprom/lhs8_state.tex}}
\end{table}
\begin{table}[H]
\centering
\caption{Pointwise $9+8$ complete coefficient-history errors
$e_q$ (\%) against the frozen linear HPROM, matching the preceding table.}
\label{tab:hprom-lhs8-coefficients}
\resizebox{\textwidth}{!}{\input{tables/euclidean_hprom/lhs8_coefficient.tex}}
\end{table}
\begin{figure}[H]
\centering
\includegraphics[width=\textwidth,height=.72\textheight,keepaspectratio]{Figures/euclidean_hprom/lhs8_solutions.pdf}
\caption{$9+8$ HPROM solution overlays, matching the cuts,
model colours, times, and common ordinate limits of
Fig.~\ref{fig:hprom-baseline-solutions}.}
\label{fig:hprom-lhs8-solutions}
\end{figure}
\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{Figures/euclidean_hprom/lhs8_coefficients.pdf}
\caption{$9+8$ per-coordinate trajectory errors, with the
same linear-HPROM reference and shared logarithmic limits as
Fig.~\ref{fig:coefficient-curves}.  All seven learned deployments appear.}
\label{fig:hprom-lhs8-coefficients}
\end{figure}
\subsection{The \texorpdfstring{$9+12$}{9+12} campaign}
\begin{table}[H]
\centering
\caption{Pointwise $9+12$ HPROM state-history errors $e_u$ (\%).
The mean averages the three in-domain reporting points.}
\label{tab:hprom-lhs12-state}
\resizebox{\textwidth}{!}{\input{tables/euclidean_hprom/lhs12_state.tex}}
\end{table}
\begin{table}[H]
\centering
\caption{Pointwise $9+12$ complete coefficient-history errors
$e_q$ (\%) against the frozen linear HPROM, matching the preceding table.}
\label{tab:hprom-lhs12-coefficients}
\resizebox{\textwidth}{!}{\input{tables/euclidean_hprom/lhs12_coefficient.tex}}
\end{table}
\begin{figure}[H]
\centering
\includegraphics[width=\textwidth,height=.72\textheight,keepaspectratio]{Figures/euclidean_hprom/lhs12_solutions.pdf}
\caption{$9+12$ HPROM solution overlays, matching the cuts,
model colours, times, and common ordinate limits of
Fig.~\ref{fig:hprom-baseline-solutions}.}
\label{fig:hprom-lhs12-solutions}
\end{figure}
\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{Figures/euclidean_hprom/lhs12_coefficients.pdf}
\caption{$9+12$ per-coordinate trajectory errors, with the
same linear-HPROM reference and shared logarithmic limits as
Fig.~\ref{fig:coefficient-curves}.  All seven learned deployments appear.}
\label{fig:hprom-lhs12-coefficients}
\end{figure}
\subsection{The \texorpdfstring{$9+18$}{9+18} campaign}
\begin{table}[H]
\centering
\caption{Pointwise $9+18$ HPROM state-history errors $e_u$ (\%).
The mean averages the three in-domain reporting points.}
\label{tab:hprom-lhs18-state}
\resizebox{\textwidth}{!}{\input{tables/euclidean_hprom/lhs18_state.tex}}
\end{table}
\begin{table}[H]
\centering
\caption{Pointwise $9+18$ complete coefficient-history errors
$e_q$ (\%) against the frozen linear HPROM, matching the preceding table.}
\label{tab:hprom-lhs18-coefficients}
\resizebox{\textwidth}{!}{\input{tables/euclidean_hprom/lhs18_coefficient.tex}}
\end{table}
\begin{figure}[H]
\centering
\includegraphics[width=\textwidth,height=.73\textheight,keepaspectratio]{Figures/euclidean_hprom/baseline_heat_absolute.pdf}
\caption{Absolute coefficient-error histories for the $9$ HPROM
campaign.  Rows follow the learned model ordering and columns the four
reporting parameters.  The dotted horizontal line marks the primary split.
The common magnitude range is stated in the panel title and is unchanged
across campaigns; hue distinguishes models, not an alternative error scale.}
\label{fig:hprom-baseline-heat-absolute}
\end{figure}
\begin{figure}[H]
\centering
\includegraphics[width=\textwidth,height=.73\textheight,keepaspectratio]{Figures/euclidean_hprom/baseline_heat_relative.pdf}
\caption{Relative coefficient-error histories for the $9$ HPROM
campaign.  Rows follow the learned model ordering and columns the four
reporting parameters.  The dotted horizontal line marks the primary split.
The common magnitude range is stated in the panel title and is unchanged
across campaigns; hue distinguishes models, not an alternative error scale.}
\label{fig:hprom-baseline-heat-relative}
\end{figure}
\begin{figure}[H]
\centering
\includegraphics[width=\textwidth,height=.73\textheight,keepaspectratio]{Figures/euclidean_hprom/lhs8_heat_absolute.pdf}
\caption{Absolute coefficient-error histories for the $9+8$ HPROM
campaign.  Rows follow the learned model ordering and columns the four
reporting parameters.  The dotted horizontal line marks the primary split.
The common magnitude range is stated in the panel title and is unchanged
across campaigns; hue distinguishes models, not an alternative error scale.}
\label{fig:hprom-lhs8-heat-absolute}
\end{figure}
\begin{figure}[H]
\centering
\includegraphics[width=\textwidth,height=.73\textheight,keepaspectratio]{Figures/euclidean_hprom/lhs8_heat_relative.pdf}
\caption{Relative coefficient-error histories for the $9+8$ HPROM
campaign.  Rows follow the learned model ordering and columns the four
reporting parameters.  The dotted horizontal line marks the primary split.
The common magnitude range is stated in the panel title and is unchanged
across campaigns; hue distinguishes models, not an alternative error scale.}
\label{fig:hprom-lhs8-heat-relative}
\end{figure}
\begin{figure}[H]
\centering
\includegraphics[width=\textwidth,height=.73\textheight,keepaspectratio]{Figures/euclidean_hprom/lhs12_heat_absolute.pdf}
\caption{Absolute coefficient-error histories for the $9+12$ HPROM
campaign.  Rows follow the learned model ordering and columns the four
reporting parameters.  The dotted horizontal line marks the primary split.
The common magnitude range is stated in the panel title and is unchanged
across campaigns; hue distinguishes models, not an alternative error scale.}
\label{fig:hprom-lhs12-heat-absolute}
\end{figure}
\begin{figure}[H]
\centering
\includegraphics[width=\textwidth,height=.73\textheight,keepaspectratio]{Figures/euclidean_hprom/lhs12_heat_relative.pdf}
\caption{Relative coefficient-error histories for the $9+12$ HPROM
campaign.  Rows follow the learned model ordering and columns the four
reporting parameters.  The dotted horizontal line marks the primary split.
The common magnitude range is stated in the panel title and is unchanged
across campaigns; hue distinguishes models, not an alternative error scale.}
\label{fig:hprom-lhs12-heat-relative}
\end{figure}
\begin{figure}[H]
\centering
\includegraphics[width=\textwidth,height=.73\textheight,keepaspectratio]{Figures/euclidean_hprom/lhs18_heat_absolute.pdf}
\caption{Absolute coefficient-error histories for the $9+18$ HPROM
campaign.  Rows follow the learned model ordering and columns the four
reporting parameters.  The dotted horizontal line marks the primary split.
The common magnitude range is stated in the panel title and is unchanged
across campaigns; hue distinguishes models, not an alternative error scale.}
\label{fig:hprom-lhs18-heat-absolute}
\end{figure}
\begin{figure}[H]
\centering
\includegraphics[width=\textwidth,height=.73\textheight,keepaspectratio]{Figures/euclidean_hprom/lhs18_heat_relative.pdf}
\caption{Relative coefficient-error histories for the $9+18$ HPROM
campaign.  Rows follow the learned model ordering and columns the four
reporting parameters.  The dotted horizontal line marks the primary split.
The common magnitude range is stated in the panel title and is unchanged
across campaigns; hue distinguishes models, not an alternative error scale.}
\label{fig:hprom-lhs18-heat-relative}
\end{figure}
\section{LSPG-Sensitive Basis Exploration}
\label{app:lspg-sensitive}
Before adopting the Euclidean POD basis used in the main text, we explored an
$M$-regularized basis metric intended to emphasize sensitivity of the LSPG
normal equations to reduced-coordinate perturbations.  The objective was to
reduce transfer from a learned secondary block to residual-solved primary
coordinates without degrading the linear reference.  In the present Burgers
campaign, controlled tests did not establish a systematic improvement
sufficient to justify making that metric the main experimental variable.  For
clarity and reproducibility, every reported result therefore uses the standard
Euclidean POD basis of \eqref{eq:euclidean-pod}.  The metric-aware
construction is retained as an exploratory direction, not as a contribution
or as a source of the numerical comparisons in this paper.  The historical
LSPG-sensitive enrichment campaign also selected an ELU master map, whereas
the Euclidean campaign uses SiLU.  Its earlier extrapolation trend is therefore
not an architecture-controlled metric comparison and cannot be attributed to
the basis choice alone.

\bibliographystyle{unsrt}
\bibliography{bibliography}
\end{document}
