\documentclass[11pt]{article}

\usepackage[a4paper,margin=1in]{geometry}
\usepackage{amsmath,amssymb,bm}
\usepackage{graphicx}
\usepackage[hypertexnames=false]{hyperref}
\usepackage{booktabs}
\usepackage{float}
\usepackage[section]{placeins}

% Title option 1:
% Nonlinear latent manifolds and closure models for reduced order models
% Title option 2:
% Learned Closures and Nonlinear Latent Manifolds for ANN-Enhanced Reduced-Order Models
\title{\vspace{-1.5em}
Robust nonlinear projection-based reduced-order models: comparative assessment of closure and manifold strategies}
\author{S. Ares de Parga, Y. Maday}
\date{\today}

\begin{document}
\maketitle
\vspace{-1.0em}

\begin{abstract}
We compare four ANN-enhanced reduced-order modeling routes under aggressive online compression: (i) closure-based intrusive ROMs (PROM-ANN), (ii) latent-manifold intrusive ROMs (PROM-POD-AE), (iii) non-intrusive coefficient-space POD-NN-ROM surrogates $(\bm\mu,t)\mapsto\mathbf q_N$, and (iv) non-intrusive latent POD-DL-ROM surrogates $(\bm\mu,t)\mapsto\mathbf z\mapsto\mathbf q_N$. For intrusive models, the online state is obtained by time-discrete least-squares Petrov--Galerkin (LSPG) minimization, solved by Gauss--Newton, and accelerated by ECSW hyperreduction. The closure family includes state-conditioned, parameter--time conditioned, and hybrid maps for truncated coefficients. The latent family evolves low-dimensional latent coordinates and reconstructs reduced coefficients through a decoder.

A central methodological choice is ROM-consistent training: learning targets are generated by a linear $n_{\mathrm{tot}}$-dimensional HPROM solve in the full truncated space, rather than by direct projection of HDM trajectories. Since closure errors in reconstructed high-order coefficients can feed back into the resolved coordinates of an intrusive LSPG solve, we also introduce a metric-aware basis construction based on an $M$-regularized LSPG sensitivity metric. This metric is used as a robustness device for the main HPROM-consistent campaign, not as a model-specific tuning parameter. We also consider a second enrichment stage built from additional low-cost HPROM trajectories at Latin-hypercube parameter points. This manuscript consolidates the mathematical formulation and experimental protocol for the $250\times250$ 2D parametric inviscid Burgers benchmark and prepares an extension to a shallow-water benchmark.
\end{abstract}

\vspace{-1.0em}

\section{Introduction}
Projection-based model order reduction (PMOR) is a standard framework for reducing high-dimensional parametric models while preserving governing equations \cite{antoulas2005approximation,quarteroni2014reduced}. For nonlinear transport-dominated dynamics, linear subspaces may require large dimensions, which motivates nonlinear manifolds, closure modeling, and latent representations \cite{barnett2023neural,cohen2023nonlinear,aghili2026nonlinear}.

From a machine-learning viewpoint, four families are relevant to this work:
\begin{itemize}
\item intrusive closure-enhanced projection (PROM-ANN) \cite{barnett2023neural},
\item intrusive latent-manifold projection (PROM-POD-AE), in the spirit of hyperreduced nonlinear manifold ROMs \cite{romor2025explicable},
\item non-intrusive direct-coefficient regression maps $(\bm\mu,t)\mapsto\mathbf q_N$,
\item non-intrusive latent POD-DL-ROM maps \cite{fresca2022pod}.
\end{itemize}

\paragraph{Naming convention used in this manuscript.}
When we say \textbf{POD-NN-ROM}, we refer to the non-intrusive coefficient-space
map $(\bm\mu,t)\mapsto\mathbf q_N$. When we say \textbf{POD-DL-ROM}, we refer to
the non-intrusive latent family in the sense of Fresca--Manzoni
\cite{fresca2022pod}. When we say \textbf{PROM-POD-AE}, we refer
to an intrusive latent-manifold projection route (conceptually aligned with
intrusive nonlinear-manifold HPROM literature such as \cite{romor2025explicable}),
even though those references use different method names.

The goal is a consistent comparison under strong online compression, using the same HDM residual, same time discretization, and the same HPROM intrusive solver/hyperreduction stack for intrusive variants.

\section{Linear HPROM Reference and Notation}
\label{sec:linear-rom}

\subsection{Generic semi-discrete IVP and residual notation}
Given the generic, parametric, nonlinear, semi-discrete, first-order initial
value problem
\begin{equation}
\mathbf M(\bm\mu)\,\dot{\mathbf u}(t;\bm\mu)
\;+\;
\mathbf f\!\left(\mathbf u(t;\bm\mu);\bm\mu\right)
\;-\;
\mathbf g(t;\bm\mu)
\;=\;\mathbf 0,
\qquad
\mathbf u(0;\bm\mu)=\mathbf u_0(\bm\mu),
\label{eq:generic-ivp}
\end{equation}
with $\bm\mu\in\mathcal D\subset\mathbb R^p$, we denote by
$\mathbf r^{m+1}$ the nonlinear time-discrete residual associated with the
selected implicit time integrator. In compact form, each HDM step is written
as
\begin{equation}
\mathbf r^{m+1}\!\left(\mathbf u^{m+1};\bm\mu\right)=\mathbf 0.
\label{eq:generic-discrete-residual}
\end{equation}
For readability, dependencies on previous time states and $\Delta t$ are kept
implicit in $\mathbf r^{m+1}$.

\subsection{POD basis from SVD}
Let $\mathbf u(t;\bm\mu)\in\mathbb R^N$ denote the HDM state with parameters $\bm\mu\in\mathcal D\subset\mathbb R^p$. Define
\begin{equation}
\mathbf u_{\mathrm{ref}}:=\frac{1}{N_s}\sum_{i=1}^{N_s}\mathbf u^i,
\qquad
\mathbf S_c=[\mathbf u^1-\mathbf u_{\mathrm{ref}}\ \cdots\ \mathbf u^{N_s}-\mathbf u_{\mathrm{ref}}].
\end{equation}
Let
\begin{equation}
\mathbf S_c=\mathbf U_{\mathbf S}\mathbf\Sigma_{\mathbf S}\mathbf Y_{\mathbf S}^T
\end{equation}
be its thin SVD, and define the linear ROB
\begin{equation}
\mathbf V_{\mathrm{tot}}\in\mathbb R^{N\times n_{\mathrm{tot}}}
\end{equation}
as the first $n_{\mathrm{tot}}$ left singular vectors of $\mathbf U_{\mathbf S}$.

The linear reduced reconstruction is
\begin{equation}
\tilde{\mathbf u}(t;\bm\mu)=\mathbf u_{\mathrm{ref}}+\mathbf V_{\mathrm{tot}}\mathbf q_N(t;\bm\mu),
\qquad
\mathbf q_N(t;\bm\mu)\in\mathbb R^{n_{\mathrm{tot}}}.
\label{eq:linear-reconstruction}
\end{equation}

\subsection{Linear time-discrete LSPG reference form}
Let $\mathbf r^{m+1}(\mathbf u;\bm\mu)\in\mathbb R^N$ denote the HDM time-discrete residual at step $m{+}1$. The unsampled reference linear LSPG solve is
\begin{equation}
\mathbf q_N^{m+1}(\bm\mu)
=
\arg\min_{\mathbf x\in\mathbb R^{n_{\mathrm{tot}}}}
\left\|
\mathbf r^{m+1}\!\left(\mathbf u_{\mathrm{ref}}+\mathbf V_{\mathrm{tot}}\mathbf x;\bm\mu\right)
\right\|_2^2.
\label{eq:linear-lspg-min}
\end{equation}

With
\begin{equation}
\mathbf J^{m+1,k}:=\frac{\partial\mathbf r^{m+1}}{\partial\mathbf u},
\qquad
\mathbf T_N:=\mathbf V_{\mathrm{tot}},
\qquad
\mathbf W_N^{m+1,k}:=\mathbf J^{m+1,k}\mathbf T_N,
\end{equation}
the Gauss--Newton increment solves
\begin{equation}
(\mathbf W_N^{m+1,k})^T\mathbf W_N^{m+1,k}\,\Delta\mathbf q_N^{m+1,k}
=
-(\mathbf W_N^{m+1,k})^T
\mathbf r^{m+1}\!\left(\mathbf u_{\mathrm{ref}}+\mathbf V_{\mathrm{tot}}\mathbf q_N^{m+1,k};\bm\mu\right).
\label{eq:linear-gn-update}
\end{equation}

\subsection{Linear HPROM via ECSW}
Assume element-wise residual decomposition
\begin{equation}
\mathbf r^{m+1}(\mathbf u;\bm\mu)
=
\sum_{e_i\in\mathcal E}
\mathbf L_{e_i}^T\,
\mathbf r_{e_i}^{m+1}(\mathbf L_{e_i^+}\mathbf u;\bm\mu).
\end{equation}
ECSW approximation yields
\begin{equation}
\widetilde{\mathbf r}^{m+1}(\mathbf x;\bm\mu)
=
\sum_{e_i\in\widetilde{\mathcal E}}\xi_{e_i}
\mathbf L_{e_i}^T\,
\mathbf r_{e_i}^{m+1}\!\left(
\mathbf L_{e_i^+}\left[\mathbf u_{\mathrm{ref}}+\mathbf V_{\mathrm{tot}}\mathbf x\right];\bm\mu
\right),
\qquad
\xi_{e_i}>0,
\end{equation}
with sampled set $\widetilde{\mathcal E}\subset\mathcal E$ \cite{farhat2015structure,de2023hyper}. The linear HPROM solve is
\begin{equation}
\mathbf q_N^{m+1}(\bm\mu)
=
\arg\min_{\mathbf x\in\mathbb R^{n_{\mathrm{tot}}}}
\left\|
\widetilde{\mathbf r}^{m+1}(\mathbf x;\bm\mu)
\right\|_2^2.
\label{eq:linear-hprom-min}
\end{equation}

\paragraph{ROM-consistent training targets.}
All learning targets in this work are extracted from trajectories
$\mathbf q_N(t;\bm\mu)$ generated by the linear HPROM solve
\eqref{eq:linear-hprom-min}. They are not obtained by orthogonal projection of
HDM snapshots. Thus, the learned maps approximate relations between
ROM-generated coordinates that satisfy the same reduced optimality structure
used online. This distinction is essential: training on projected HDM
coefficients would mix a projection target with an online LSPG/HPROM dynamics,
whereas the present protocol keeps data generation, training, and online
evaluation consistent. Equation~\eqref{eq:linear-lspg-min} is retained only as
the unsampled LSPG reference form.

\section{Metric-Aware Basis Construction for Closure Robustness}
\label{sec:metric-aware-basis}

The closure models considered below depend on a split
$\mathbf V_{\mathrm{tot}}=[\mathbf V\ \overline{\mathbf V}]$ between online
coordinates $\mathbf q$ and reconstructed coordinates $\bar{\mathbf q}$. This
split is not only an approximation choice: in an intrusive LSPG solve,
errors in the reconstructed high-order coordinates can feed back into the
low-order coordinates through the residual normal equations. Consequently, the
metric used to construct $\mathbf V_{\mathrm{tot}}$ becomes part of the
robustness design.

\subsection{Euclidean and M-regularized LSPG-sensitive metrics}
The baseline basis is the usual Euclidean POD basis obtained from the centered
snapshot matrix $\mathbf S_c$. We also consider a residual-sensitive metric
that combines a physical mass contribution with an averaged LSPG sensitivity:
\begin{equation}
\mathbf W
=
\varepsilon\,\mathbf M
+
\sum_{s\in\mathcal S_{\mathrm{met}}}
\omega_s\,
(\mathbf J_s)^T\mathbf P_s\mathbf J_s .
\label{eq:mlspg-metric}
\end{equation}
Here $\mathbf M$ is the block mass/area matrix associated with the state
inner product, $\mathbf J_s=\partial\mathbf r_s/\partial\mathbf u$ is the
time-discrete residual Jacobian at a selected snapshot, $\mathbf P_s$ is the
nonnegative sampling/weighting operator used for the residual metric, and
$\omega_s$ are quadrature and parameter weights. The small positive parameter
$\varepsilon$ regularizes the metric and preserves physical state weighting
when the residual sensitivity is weak or locally rank deficient.

The corresponding weighted POD basis is obtained from the generalized
correlation matrix
\begin{equation}
\mathbf C
=
\mathbf S_c^T\mathbf W\mathbf S_c .
\label{eq:mlspg-correlation}
\end{equation}
If $\mathbf C\mathbf y_i=\lambda_i\mathbf y_i$, the associated
modes are
\begin{equation}
\bm\phi_i
=
\frac{1}{\sqrt{\lambda_i}}\mathbf S_c\mathbf y_i,
\qquad
\bm\phi_i^T\mathbf W\bm\phi_j=\delta_{ij}.
\label{eq:weighted-pod-modes}
\end{equation}
In practice, the metric is assembled on a representative subset of parameter
and time samples, selected independently from the final test points. The
resulting basis is then used uniformly by all reduced models; it is not tuned
separately for a particular closure map.

\subsection{Low/high transfer diagnostic}
To quantify why such a metric may matter for Case~2, consider a local
linearization of the LSPG normal equations around a reference reduced
trajectory. Let
\begin{equation}
\mathbf A_s=(\mathbf J_s)^T\mathbf P_s\mathbf J_s,
\qquad
\mathbf H_s=
\begin{bmatrix}
\mathbf V^T\mathbf A_s\mathbf V
&
\mathbf V^T\mathbf A_s\overline{\mathbf V}
\\
\overline{\mathbf V}^T\mathbf A_s\mathbf V
&
\overline{\mathbf V}^T\mathbf A_s\overline{\mathbf V}
\end{bmatrix}.
\end{equation}
If the high coordinates are perturbed by $\delta\bar{\mathbf q}$, the
corresponding first-order low-coordinate response is approximated by
\begin{equation}
\delta\mathbf q
\approx
-
\left(\mathbf H_{LL}\right)^{-1}\mathbf H_{LH}\,\delta\bar{\mathbf q}
=
\mathbf T_{LH}\,\delta\bar{\mathbf q}.
\label{eq:low-high-transfer}
\end{equation}
Thus, $\mathbf T_{LH}$ measures which high-coordinate directions are most
dangerous for the resolved LSPG coordinates. The purpose of the
$M$-regularized LSPG-sensitive metric is not to optimize a single regression
model, but to construct a trial basis for which this low/high transfer is less
amplifying while preserving the accuracy of the remaining model families.

\subsection{When the metric is needed}
Table~\ref{tab:metric-role-by-model} summarizes the role of the metric for the
model classes considered in this paper. The guiding principle is conservative:
the LSPG-sensitive basis is justified only if it improves the robustness of
closure-based intrusive models without degrading the linear reference,
PROM-ANN Cases~1 and~3, PROM-POD-AE, or the non-intrusive baselines.

\begin{table}[H]
\centering
\caption{Role of the basis metric in each model family.}
\label{tab:metric-role-by-model}
\resizebox{\textwidth}{!}{
\begin{tabular}{lll}
\toprule
Model family & Why the metric may matter & Practical criterion \\
\midrule
Linear HPROM &
Changes the reduced trial space and therefore the reference trajectory. &
Must not degrade the baseline projection/HPROM accuracy appreciably. \\
PROM-ANN Case 1, $\bar{\mathbf q}=\mathcal N(\mathbf q)$ &
Closure errors are tied to the online low coordinates. &
Check that the modified coordinates remain learnable and stable. \\
PROM-ANN Case 2, $\bar{\mathbf q}=\mathcal M(\bm\mu,t)$ &
High coordinates are injected independently of the online solve. &
Most sensitive case; reduce low/high transfer in \eqref{eq:low-high-transfer}. \\
PROM-ANN Case 3, $\bar{\mathbf q}=\mathcal H(\mathbf q,\bm\mu,t)$ &
Hybrid inputs may compensate part of the closure error. &
Verify that robustness improves without increasing online stiffness. \\
PROM-POD-AE &
The decoder acts on coordinates defined by the selected basis. &
Preserve latent reconstruction quality and LSPG convergence. \\
POD-NN-ROM/POD-DL-ROM &
No intrusive feedback, but the regression targets change. &
Assess coefficient and state errors; do not optimize only for Case~2. \\
\bottomrule
\end{tabular}
}
\end{table}

The numerical evidence used to select the $M$-regularized LSPG-sensitive
basis for the Burgers campaign is reported later, after the benchmark has been
introduced, in Appendix~\ref{app:basis-selection-diagnostics}.  It consists of
the direct low/high transfer measure in \eqref{eq:low-high-transfer} and an
oracle high-mode perturbation check.  Unless explicitly stated otherwise, the
subsequent HPROM-consistent Burgers pipeline uses this LSPG-sensitive basis as
the main reduced trial basis.

\section{Method I: PROM-ANN Closure Models}
\label{sec:method-ann}

\subsection{Manifold definitions}
For closure models, we introduce the partition
\begin{equation}
\mathbf V_{\mathrm{tot}}=[\mathbf V\ \overline{\mathbf V}],
\qquad
\mathbf q_N=
\begin{bmatrix}
\mathbf q\\
\bar{\mathbf q}
\end{bmatrix},
\qquad
n+\bar n=n_{\mathrm{tot}}.
\end{equation}

PROM-ANN uses retained coordinates $\mathbf q\in\mathbb R^n$ as online unknowns and reconstructs truncated coordinates via learned maps:
\begin{align}
\text{Case 1:}&\quad \bar{\mathbf q}=\mathcal N(\mathbf q),\\
\text{Case 2:}&\quad \bar{\mathbf q}=\mathcal M(\bm\mu,t),\\
\text{Case 3:}&\quad \bar{\mathbf q}=\mathcal H(\mathbf q,\bm\mu,t).
\end{align}

In the $n_{\mathrm{tot}}$-dimensional linear HPROM used for target generation,
both $\mathbf q$ and $\bar{\mathbf q}$ are solved online. In the PROM-ANN
models, only $\mathbf q$ remains an online unknown; the secondary coordinates
are injected by the learned closure. This gives an $n$-dimensional nonlinear
trial manifold embedded in the same $n_{\mathrm{tot}}$-dimensional reduced
space used to generate the training data.

The state approximation is
\begin{equation}
\tilde{\mathbf u}(t;\bm\mu)
=
\mathbf u_{\mathrm{ref}}+
\mathbf V\,\mathbf q(t;\bm\mu)+
\overline{\mathbf V}\,\mathcal F(\mathbf q,\bm\mu,t),
\label{eq:ann-manifold}
\end{equation}
with $\mathcal F\in\{\mathcal N,\mathcal M,\mathcal H\}$.

The three cases differ in how the closure interacts with the LSPG tangent. In
Case~1 and Case~3 the tangent contains
$\overline{\mathbf V}\,\partial\mathcal F/\partial\mathbf q$, whereas in
Case~2 the closure is parameter--time conditioned and the tangent with respect
to the online coordinate is simply $\mathbf V$. Case~2 is therefore the
cheapest intrusive closure, but it is also the most exposed to high-coordinate
injection errors because the injected block is not corrected through a
state-dependent tangent.

\subsection{LSPG/Gauss--Newton solve}
At each time step,
\begin{equation}
\mathbf q^{m+1}(\bm\mu)
=
\arg\min_{\mathbf x\in\mathbb R^n}
\left\|
\mathbf r^{m+1}(\tilde{\mathbf u}^{m+1}(\mathbf x;\bm\mu);\bm\mu)
\right\|_2^2.
\label{eq:ann-lspg-min}
\end{equation}

Define
\begin{equation}
\mathbf J^{m+1,k}:=\frac{\partial \mathbf r^{m+1}}{\partial \mathbf u},
\qquad
\mathbf T_q^{m+1,k}:=\frac{\partial \tilde{\mathbf u}^{m+1}}{\partial \mathbf q}
=\mathbf V+\overline{\mathbf V}\frac{\partial\mathcal F}{\partial\mathbf q},
\qquad
\mathbf W_q^{m+1,k}=\mathbf J^{m+1,k}\mathbf T_q^{m+1,k}.
\end{equation}
The Gauss--Newton increment solves
\begin{equation}
(\mathbf W_q^{m+1,k})^T\mathbf W_q^{m+1,k}\,\Delta\mathbf q^{m+1,k}
=
-(\mathbf W_q^{m+1,k})^T
\mathbf r^{m+1}(\tilde{\mathbf u}^{m+1}(\mathbf q^{m+1,k};\bm\mu);\bm\mu).
\end{equation}

\subsection{ECSW hyperreduced form}
PROM-ANN HPROM solves
\begin{equation}
\mathbf q^{m+1}(\bm\mu)
=
\arg\min_{\mathbf x\in\mathbb R^n}
\left\|\widetilde{\mathbf r}^{m+1}(\mathbf x;\bm\mu)\right\|_2^2,
\end{equation}
with test basis $\widetilde{\mathbf W}_q=\widetilde{\mathbf J}\mathbf T_q$.

\paragraph{Offline training targets.}
For Cases 1--3, supervised data are extracted from ROM-consistent trajectories
$\mathbf q_N=[\mathbf q;\bar{\mathbf q}]$. Case~1 trains $\mathcal N$ from
$(\mathbf q,\bar{\mathbf q})$, Case~2 trains $\mathcal M$ from
$(\bm\mu,t,\bar{\mathbf q})$, and Case~3 trains $\mathcal H$ from
$(\mathbf q,\bm\mu,t,\bar{\mathbf q})$.

\section{Method II: PROM-POD-AE Latent Manifold}
\label{sec:method-podae}

\subsection{Autoencoder and latent manifold}
Let
\begin{equation}
\mathbf z=\mathcal E(\mathbf q_N),
\qquad
\widehat{\mathbf q}_N=\mathcal D(\mathbf z),
\qquad
\mathbf z\in\mathbb R^{n_z},\ \ n_z\ll n_{\mathrm{tot}}.
\end{equation}
The intrusive latent manifold is
\begin{equation}
\tilde{\mathbf u}(t;\bm\mu)
=
\mathbf u_{\mathrm{ref}}+\mathbf V_{\mathrm{tot}}\,\mathcal D(\mathbf z(t;\bm\mu)).
\label{eq:podae-manifold}
\end{equation}
This is an intrusive latent-manifold projection approach, in line with intrusive nonlinear-manifold HPROM literature \cite{romor2025explicable}, and is separated from the non-intrusive POD-DL-ROM line \cite{fresca2022pod}.

\paragraph{Offline autoencoder training.}
Given ROM-consistent targets $\mathbf q_N^{\mathrm{ref}}$, the autoencoder
$(\mathcal E,\mathcal D)$ is trained by reconstruction in reduced coordinates:
\begin{equation}
\mathcal L_{\mathrm{AE}}
=
\frac{1}{N_{\mathrm{samp}}}
\sum_{j=1}^{N_{\mathrm{samp}}}
\left\|
\mathbf q_{N,j}^{\mathrm{ref}}
-
\mathcal D\!\left(\mathcal E(\mathbf q_{N,j}^{\mathrm{ref}})\right)
\right\|_2^2.
\end{equation}
The resulting decoder $\mathcal D$ defines the nonlinear trial manifold used in
the intrusive online LSPG solve \eqref{eq:podae-lspg-min}.

\subsection{LSPG/Gauss--Newton solve in latent coordinates}
The online unknown is $\mathbf z^{m+1}\in\mathbb R^{n_z}$:
\begin{equation}
\mathbf z^{m+1}(\bm\mu)
=
\arg\min_{\mathbf y\in\mathbb R^{n_z}}
\left\|
\mathbf r^{m+1}(\mathbf u_{\mathrm{ref}}+\mathbf V_{\mathrm{tot}}\mathcal D(\mathbf y);\bm\mu)
\right\|_2^2.
\label{eq:podae-lspg-min}
\end{equation}

Define
\begin{equation}
\mathbf T_z^{m+1,k}:=\frac{\partial\tilde{\mathbf u}^{m+1}}{\partial\mathbf z}
=\mathbf V_{\mathrm{tot}}\frac{\partial\mathcal D}{\partial\mathbf z},
\qquad
\mathbf W_z^{m+1,k}=\mathbf J^{m+1,k}\mathbf T_z^{m+1,k}.
\end{equation}
The Gauss--Newton step solves
\begin{equation}
(\mathbf W_z^{m+1,k})^T\mathbf W_z^{m+1,k}\,\Delta\mathbf z^{m+1,k}
=
-(\mathbf W_z^{m+1,k})^T
\mathbf r^{m+1}(\tilde{\mathbf u}^{m+1}(\mathbf z^{m+1,k};\bm\mu);\bm\mu).
\end{equation}

\subsection{ECSW hyperreduced form}
PROM-POD-AE HPROM solves
\begin{equation}
\mathbf z^{m+1}(\bm\mu)
=
\arg\min_{\mathbf y\in\mathbb R^{n_z}}
\left\|\widetilde{\mathbf r}^{m+1}(\mathbf y;\bm\mu)\right\|_2^2,
\end{equation}
with test basis $\widetilde{\mathbf W}_z=\widetilde{\mathbf J}\mathbf T_z$.

\section{Method III: Non-Intrusive POD-NN-ROM}
\label{sec:method-datadriven}

\paragraph{Model definition.}
The non-intrusive baseline maps exogenous inputs directly to reduced coefficients:
\begin{equation}
\mathbf q_N(t;\bm\mu)\approx\mathcal G_q(\bm\mu,t).
\label{eq:data-driven-map-q}
\end{equation}
This model does not require online residual minimization.

\paragraph{Training objective and online cost.}
Given ROM-consistent targets $\mathbf q_N^{\mathrm{ref}}(t;\bm\mu)$, one minimizes a supervised loss (e.g., MSE)
\begin{equation}
\mathcal L_{\mathrm{data}}
=\frac{1}{N_{\mathrm{samp}}}\sum_{j=1}^{N_{\mathrm{samp}}}
\left\|\mathcal G_q(\bm\mu_j,t_j)-\mathbf q_{N,j}^{\mathrm{ref}}\right\|_2^2.
\end{equation}
Online prediction is then a direct forward evaluation of $\mathcal G_q$ followed by reconstruction with $\mathbf V_{\mathrm{tot}}$.

\section{Method IV: POD-DL-ROM (Latent, Non-Intrusive)}
\label{sec:method-datadriven-latent}

\paragraph{Model definition.}
The second non-intrusive baseline follows the POD-DL-ROM family \cite{fresca2022pod}:
\begin{equation}
\mathbf z(t;\bm\mu)\approx\mathcal G_z(\bm\mu,t),\qquad
\mathbf q_N(t;\bm\mu)\approx\mathcal D\!\left(\mathcal G_z(\bm\mu,t)\right),
\label{eq:data-driven-map-z}
\end{equation}
where $\mathcal D$ is a decoder trained offline and $\mathbf z\in\mathbb R^{n_z}$.

\paragraph{Training objective and online cost.}
In the present formulation, the autoencoder block $(\mathcal E,\mathcal D)$ follows the
same training strategy and normalization used in
Section~\ref{sec:method-podae}: reconstruction of ROM-consistent
$\mathbf q_N$ snapshots through a latent bottleneck. The POD-DL-ROM dynamics map then
replaces the POD-NN-ROM output variable $\mathbf q_N$ by $\mathbf z$, i.e., it learns
$(\bm\mu,t)\mapsto\mathbf z$ and composes with the same decoder $\mathcal D$:
\begin{equation}
\mathbf q_N(\bm\mu,t)\approx \mathcal D\!\left(\mathcal G_z(\bm\mu,t)\right).
\end{equation}
Hence, POD-NN-ROM and POD-DL-ROM share the same exogenous-input principle
$(\bm\mu,t)\mapsto(\cdot)$, with POD-DL-ROM acting in latent coordinates as proposed in
\cite{fresca2022pod}. Online evaluation remains non-intrusive (single forward pass)
and does not involve residual minimization.

\section{Physics-Regularized Variant (Ancillary)}
\label{sec:prnn-note}

Physics-regularized neural training adds a residual-based term to a supervised objective \cite{chen2021physics,sibuet2025discrete}. In this study, such variants are treated as optional diagnostics (not core baselines) because they remain significantly more expensive offline and do not consistently improve accuracy in the present benchmark.

\section{Burgers Benchmark and Experimental Protocol}
\label{sec:burgers}

\subsection{Governing equations and parameter domain}
We use the 2D parametric inviscid Burgers benchmark from \cite{barnett2023neural},
in conservative vector form:
\begin{equation}
\frac{\partial \mathbf U}{\partial t}
+\frac{\partial \mathbf F(\mathbf U)}{\partial x}
+\frac{\partial \mathbf G(\mathbf U)}{\partial y}
=\mathbf S(x;\bm\mu),
\qquad
\mathbf U=
\begin{bmatrix}
u_x\\ u_y
\end{bmatrix},
\end{equation}
with
\begin{equation}
\mathbf F(\mathbf U)=
\begin{bmatrix}
\tfrac{1}{2}u_x^2\\
\tfrac{1}{2}u_xu_y
\end{bmatrix},
\qquad
\mathbf G(\mathbf U)=
\begin{bmatrix}
\tfrac{1}{2}u_xu_y\\
\tfrac{1}{2}u_y^2
\end{bmatrix},
\qquad
\mathbf S(x;\bm\mu)=
\begin{bmatrix}
0.02\,\exp(\mu_2 x)\\
0
\end{bmatrix}.
\end{equation}
The problem is defined on $\Omega=[0,100]\times[0,100]$ with final time
$T_f=25$ and parameters $\bm\mu=(\mu_1,\mu_2)\in\mathcal D$, where
\begin{equation}
\mathcal D=[4.25,5.50]\times[0.015,0.03].
\end{equation}
This conservative form is the one used by the discrete residual and Jacobian in
the ROM/HPROM pipeline.
The initial/boundary conditions used in this benchmark are
\begin{equation}
u_x(0,y,t;\bm\mu)=\mu_1,\qquad
u_x(x,y,0)=1,\qquad
u_y(x,y,0)=1.
\end{equation}

\subsection{Numerical discretization}
The HDM uses a cell-centered finite-volume Godunov discretization on a
$250\times250$ mesh, hence $N=2N_xN_y=125{,}000$. Time integration uses
$500$ uniform steps with $\Delta t=0.05$ (thus $501$ stored states including
$t=0$). The same time-discrete residual is used by linear HPROM and by all
intrusive ANN-enhanced variants (PROM-ANN and PROM-POD-AE) through
LSPG/Gauss--Newton solves.

\subsection{Training and evaluation protocol}
Stage 1 builds $\mathbf V_{\mathrm{tot}}$ from HDM snapshots at $9$ structured
training parameters (a $3\times3$ tensor grid on $\mathcal D$), giving
$N_s=9\times501=4509$ snapshots. Stage 2 generates ROM-consistent coefficients
$\mathbf q_N(t;\bm\mu)$ by solving the linear full-space reduced model
($n_{\mathrm{tot}}$). Stage 3 trains the learned maps from these reduced
targets.

Primary evaluation is reported at one verification point and two off-grid
points:
\begin{equation}
\bm\mu^{(v)}=(4.875,0.0225),\qquad
\bm\mu^{(1)}=(4.56,0.019),\qquad
\bm\mu^{(2)}=(5.19,0.026).
\end{equation}
For the expanded-enrichment study, we also add an extrapolatory stress-test
point located outside the original training box,
\begin{equation}
\bm\mu^{(3)}=(4.00,0.0330),
\end{equation}
which is $20\%$ beyond the upper-left corner of $\mathcal D$ in both parameter
directions. Throughout the tables and figures, the superscript $(v)$ denotes
the verification point, $(1)$ and $(2)$ denote the two off-grid test points,
and $(3)$ denotes the extrapolatory stress-test point. Results are presented in
the order $\bm\mu^{(v)}$, $\bm\mu^{(1)}$, $\bm\mu^{(2)}$, and
$\bm\mu^{(3)}$ in the main baseline and enriched result tables and figures.

In addition to the baseline $3\times3$ training set, we consider an expanded
enriched campaign with $36$ Latin-hypercube points generated by the fixed
linear HPROM.  The enrichment contains $18$ points inside the original domain
$\mathcal D$ and $18$ points in a $25\%$ expanded parameter box,
\begin{equation}
[3.9375,5.8125]\times[0.01125,0.03375].
\end{equation}
No additional HDM trajectories are computed.  The enriched Stage~2 dataset
therefore contains $45$ ROM-consistent trajectories, or
$45\times501=22{,}545$ coefficient samples.  The enrichment run keeps the same
MLSPG-sensitive basis and reuses the fixed Stage--2 linear ECSW weights from
the baseline campaign; the linear ECSW rule is not copied, rebuilt, or retuned.
The four evaluation points are excluded from the enrichment set.  Figures
\ref{fig:parameter-domain-sampling} and
\ref{fig:parameter-domain-enrichment} summarize the baseline and expanded
enriched sampling sets.

\begin{figure}[H]
\centering
\includegraphics[width=0.82\textwidth]{Figures/parameter_domain_sampling_points_with_mu3.png}
\caption{Baseline training set and evaluation points in parameter space.  The axes are shared with Figure~\ref{fig:parameter-domain-enrichment} so that the location of the extrapolatory stress-test point $\bm\mu^{(3)}$ is shown on the same scale as the expanded enrichment.}
\label{fig:parameter-domain-sampling}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.82\textwidth]{Figures/mlspg_hprom_enrichment_ext25_lhs36/parameter_domain_extended_enrichment_points.png}
\caption{Expanded enriched MLSPG-sensitive campaign: baseline $3\times3$ grid, $18$ interior LHS points, $18$ margin LHS points generated with the fixed linear HPROM, and the four evaluation points.  The axes match Figure~\ref{fig:parameter-domain-sampling}; the margin points lie in the $25\%$ expanded sampling box around the original domain $\mathcal D$.}
\label{fig:parameter-domain-enrichment}
\end{figure}

\subsection{HPROM-Consistent Evaluation Pipeline}
To avoid protocol-induced bias in intrusive comparisons, we enforce one
end-to-end HPROM pipeline: Stage~2 reduced targets are generated with linear HPROM in
$\mathbf V_{\mathrm{tot}}$, Stage~3 intrusive models are trained on these
HPROM-consistent reduced trajectories, and online intrusive evaluations are
performed with linear HPROM settings. This applies uniformly to Linear HPROM,
PROM-ANN (Cases~1--3), and PROM-POD-AE.

\subsection{Reported Metrics}
For each evaluation parameter, we report complementary state-space,
coefficient-space, and runtime metrics. State accuracy is measured by the
relative space-time trajectory error versus HDM (defined in
Section~\ref{sec:burgers-results}, Error Definitions). Coefficient-space
comparisons are reported against the linear $n_{\mathrm{tot}}$ reference,
including per-index absolute/relative modal errors and time-resolved modal
error heatmaps. Computational performance is reported through online runtimes
and speedups relative to the HDM mean runtime.

\section{Burgers Results in the HPROM-Consistent Setting}
\label{sec:burgers-results}

This section reports the Burgers results for the completed model classes and
configurations considered here: the linear HPROM reference, PROM-ANN
Cases~1--3, intrusive PROM-POD-AE, non-intrusive POD-NN-ROM
$(\bm\mu,t)\mapsto\mathbf q_N$, and non-intrusive POD-DL-ROM.

\subsection{Error Definitions}

The trajectory error reported in tables and summaries is the relative space-time
Frobenius error (in \%):
\begin{equation}
\varepsilon_u^{(m)}(\bm\mu)
=
100\,
\frac{\left\|
\mathbf U_{\mathrm{HDM}}(\bm\mu)-\mathbf U_m(\bm\mu)
\right\|_F}
{\left\|
\mathbf U_{\mathrm{HDM}}(\bm\mu)
\right\|_F},
\label{eq:traj-error-def}
\end{equation}
where $\mathbf U_{\mathrm{HDM}},\mathbf U_m\in\mathbb R^{N\times N_t}$ collect
all states along time.

Coefficient diagnostics use the linear $n_{\mathrm{tot}}$-HPROM coefficients
$\mathbf Q_{\mathrm{ref}}=[\mathbf q_{\mathrm{ref}}(t_1)\cdots\mathbf q_{\mathrm{ref}}(t_{N_t})]$
as reference and model coefficients $\mathbf Q_m$:
\begin{equation}
\varepsilon_{q_i,\mathrm{abs}}^{(m)}
=
\left\|q_{\mathrm{ref},i}(\cdot)-q_{m,i}(\cdot)\right\|_2,
\qquad
\varepsilon_{q_i,\mathrm{rel}}^{(m)}
=
\frac{\left\|q_{\mathrm{ref},i}(\cdot)-q_{m,i}(\cdot)\right\|_2}
{\left\|q_{\mathrm{ref},i}(\cdot)\right\|_2},
\label{eq:coeff-error-def}
\end{equation}
and heatmaps display
\begin{equation}
e_{i,k}^{(m)}=
\left|q_{\mathrm{ref},i}(t_k)-q_{m,i}(t_k)\right|,
\qquad
\widehat e_{i,k}^{(m)}=
\frac{\left|q_{\mathrm{ref},i}(t_k)-q_{m,i}(t_k)\right|}
{\left\|q_{\mathrm{ref},i}(\cdot)\right\|_2}.
\label{eq:heatmap-error-def}
\end{equation}

For online cost tables, intrusive methods are timed with their online
Gauss--Newton solve, while non-intrusive methods are timed through pure network
inference. Speedup is defined with respect to the mean HDM runtime per
parameter,
\begin{equation}
S^{(m)}=\frac{t_{\mathrm{HDM}}}{t_m},
\label{eq:speedup-def}
\end{equation}
where the current campaign uses $t_{\mathrm{HDM}}=737.44$ s, obtained from the
mean runtime over the nine HDM training trajectories.


\subsection{Current MLSPG-Sensitive HPROM Campaign}

The first production campaign uses the $M$-regularized LSPG-sensitive basis
introduced above and the HPROM backend generated from this basis.  The
completed non-enriched campaign contains the linear HPROM, PROM-ANN
Cases~1--3, intrusive PROM-POD-AE, non-intrusive POD-NN-ROM, and non-intrusive
POD-DL-ROM.  Every reported state and coefficient trajectory was generated
with the same MLSPG-sensitive basis and the same Stage~2 linear-HPROM training
dataset; no result from the earlier Euclidean/PROM exploratory campaign is
included.

Table~\ref{tab:mlspg-hprom-current-training} reports the Stage--3 networks
selected for the non-enriched campaign in reader-facing form.  The PROM-ANN
and POD-NN-ROM rows use the same feed-forward MLP widths, while PROM-POD-AE
and POD-DL-ROM use explicit latent bottlenecks with $n_z=10$.

\input{tables/mlspg_hprom_current_training_winners.tex}

\input{tables/mlspg_hprom_current_errors.tex}

On the original verification/off-grid points
$\bm\mu^{(v)}$, $\bm\mu^{(1)}$, and $\bm\mu^{(2)}$, the 2\% ECSW learned
intrusive rows remain close to the fixed linear HPROM for Case~1, Case~3, and
PROM-POD-AE.  PROM-ANN Case~3 gives errors between $0.415\%$ and $0.608\%$,
Case~1 gives $0.424\%$--$0.722\%$, and PROM-POD-AE gives
$0.458\%$--$0.689\%$, compared with the linear-HPROM range
$0.413\%$--$0.472\%$.  By contrast, the parameter--time Case~2 closures remain
more fragile off grid.  Case~2 with $n=10$ reaches $1.808\%$ and $1.565\%$ on
the two off-grid points, while Case~2 with $n=20$ reaches $3.433\%$ and
$5.569\%$.  Thus, increasing the number of resolved primary coordinates does
not, by itself, improve the parameter-to-tail closure.  Appendix
\ref{app:case2-trimmed-diagnostic} reports an additional diagnostic in which
the stronger $n=10$ Case~2 map is trimmed and reused inside an $n=20$ online
solve, to separate the effect of the online split from the effect of
independently training the $n=20$ closure.  Among the non-intrusive models,
POD-DL-ROM improves on POD-NN-ROM at both off-grid points and remains
comparable at the verification point.

The extrapolatory point $\bm\mu^{(3)}$ is intentionally harder and is not
included in the original-three mean used in the enrichment comparison.  At
this point the linear HPROM remains below $0.9\%$, while the learned models
trained only on the original coefficient set degrade more strongly:
PROM-ANN Case~1 reaches $1.563\%$, Case~2 reaches $4.313\%$ for $n=10$ and
$6.440\%$ for $n=20$, Case~3 reaches $1.438\%$, PROM-POD-AE reaches
$1.943\%$, POD-NN-ROM reaches $9.192\%$, and POD-DL-ROM reaches $4.980\%$.
This behavior motivates the expanded
HPROM-generated coefficient enrichment reported below.

To separate the sources of error in the learned intrusive rows, we also run a
diagnostic that compares three levels for the same trained nonlinear
representations: direct reconstruction from the saved linear-HPROM coefficient
trajectory, a full-residual PROM solve without ECSW sampling, and the production
2\% ECSW HPROM solve.  This diagnostic is not a replacement for the production
tables, because the reconstruction-only rows do not solve the online residual.
However, it is useful for identifying whether a loss of accuracy is already
present in the learned reconstruction map, appears when the reduced coordinates
are advanced by the nonlinear PROM dynamics, or is introduced by ECSW sampling.

\input{tables/mlspg_recon_prom_hprom_diagnostic.tex}

Table~\ref{tab:mlspg-recon-prom-hprom-aggregate} shows that PROM-ANN
Cases~1 and~3 have small reconstruction floors on the original three points,
and their full PROM and HPROM errors remain close to those floors.  Case~2 is
different: a substantial part of the error is already present in the
parameter--time high-coordinate prediction, and the HPROM sampling adds a
further penalty, especially at $\bm\mu^{(3)}$.  PROM-POD-AE should be read
separately: encoding and decoding the linear-HPROM coefficient trajectory is
not its best latent representation, so the reconstruction-only row is not a
lower bound.  Its full PROM solve finds substantially better latent coordinates
and the 2\% HPROM row stays close to that full-PROM value.

The hyperreduction information for the same campaign is reported in
Table~\ref{tab:mlspg-hprom-current-hyperreduction}.  The linear HPROM uses the
shared Stage~2 ECSW weights, while the intrusive PROM-ANN models use
case-specific ECSW weights trained for their corresponding nonlinear residuals.
PROM-POD-AE uses its own latent-manifold ECSW rule, constructed with the
decoder tangent entering the reduced residual.  For the intrusive learned
models, the ECSW training set is not a contiguous block of time steps.  It is
selected by a parameter--time stratified sampling rule:
all available training parameter trajectories are represented and the sampled
time indices are spread over the transient, with a shifted time grid used to
avoid repeatedly selecting the same temporal locations.  The nominal sampling
fraction therefore controls the size of the residual/tangent training set used
to identify the element weights; the final online mesh size $N_e$ is the number
of elements retained with nonzero ECSW weights after the nonnegative least
squares solve.

The reported learned intrusive rows use 2\% case-specific ECSW rules.  This
choice is used consistently for PROM-ANN Cases~1--3 and PROM-POD-AE in both
the baseline and enriched campaigns.  The linear HPROM is not rebuilt for this
comparison; it uses the fixed Stage~2 ECSW rule that defines the coefficient
teacher.  POD-NN-ROM and POD-DL-ROM are included for timing comparison but are
non-intrusive and therefore do not require ECSW.

\input{tables/mlspg_hprom_current_hyperreduction.tex}

The average online timings in Table
\ref{tab:mlspg-hprom-current-hyperreduction} include the four reported
evaluation points.  PROM-POD-AE retains $901$ elements and averages
$8.57$ s per trajectory, corresponding to an $86.0\times$ speedup over HDM.
PROM-ANN Case~2 with $n=10$ is the fastest intrusive model at $2.63$ s
($280.9\times$), while Case~2 with $n=20$ retains $1801$ elements and averages
$3.97$ s ($185.8\times$).  Case~1 and Case~3 both average about $9.1$ s, with
speedups near $81\times$.  The non-intrusive online costs are $0.0322$ s for
POD-NN-ROM and $0.0206$ s for POD-DL-ROM.  These online speedups do not
include the common offline costs of HDM snapshot generation, linear-HPROM
target generation, or network training.

\subsection{Solution Overlays}

Figure~\ref{fig:mlspg-hprom-current-overlays} compares the HDM, the linear
HPROM, all selected intrusive learned HPROMs, and both non-intrusive baselines
at the four reported evaluation points.  Solid curves correspond to the final
time, while dashed curves show intermediate times.  The line cuts are the
horizontal and vertical midline cuts of the $u_x$ component.

\begin{figure}[H]
\centering
\includegraphics[width=0.98\textwidth]{Figures/mlspg_hprom_current/mlspg_hprom_solution_overlays.png}
\caption{Current MLSPG-sensitive campaign: HDM, linear HPROM, selected PROM-ANN HPROMs, PROM-POD-AE with 2\% ECSW, POD-NN-ROM, and POD-DL-ROM. Rows are ordered as verification point $\bm\mu^{(v)}$, off-grid points $\bm\mu^{(1)}$ and $\bm\mu^{(2)}$, and extrapolatory point $\bm\mu^{(3)}$.}
\label{fig:mlspg-hprom-current-overlays}
\end{figure}

\subsection{Coefficient-Space Diagnostics}

All coefficient-space diagnostics in this section use the MLSPG-sensitive
linear HPROM coefficient trajectories as the reference:
\begin{equation}
\mathbf Q_{\mathrm{ref}}(\bm\mu)
=
\mathbf Q_{\mathrm{lin}}^{\mathrm{MLSPG\text{-}HPROM}}(\bm\mu).
\end{equation}
This is the relevant coefficient reference because the learned maps are trained
from the same $\mathbf V_{\mathrm{tot}}$ basis and from the same MLSPG-sensitive
linear HPROM trajectories.  For all completed models in this campaign, the
coefficient diagnostics use the solver-side saved coordinates $\mathbf q_N(t)$
directly.  No least-squares post-projection of state snapshots is used for the
reported coefficient figures.

Figure~\ref{fig:mlspg-hprom-current-coeff-curves} reports the per-index
coefficient errors against this MLSPG-linear reference.  The vertical dotted
lines indicate the primary/secondary split for the two Case~2 configurations.

\begin{figure}[H]
\centering
\includegraphics[width=0.98\textwidth]{Figures/mlspg_hprom_current/coeff_errors/mlspg_hprom_coeff_abs_rel_all_points.png}
\caption{Current MLSPG-sensitive HPROM campaign: per-coefficient absolute and relative errors with respect to the MLSPG-sensitive linear HPROM coefficient trajectories. Panels are ordered as $\bm\mu^{(v)}$, $\bm\mu^{(1)}$, $\bm\mu^{(2)}$, and $\bm\mu^{(3)}$.}
\label{fig:mlspg-hprom-current-coeff-curves}
\end{figure}

Figures~\ref{fig:mlspg-hprom-current-abs-heatmaps} and
\ref{fig:mlspg-hprom-current-rel-heatmaps} show the corresponding time-resolved
coefficient-error heatmaps.  The horizontal axis is time and the vertical axis
is the coefficient index.  The color scale is clipped at the 99th percentile to
make the dominant error structures visible without being controlled by isolated
outliers.

\begin{figure}[H]
\centering
\includegraphics[width=0.98\textwidth]{Figures/mlspg_hprom_current/coeff_errors/mlspg_hprom_coeff_abs_heatmaps.png}
\caption{Current MLSPG-sensitive HPROM campaign: absolute coefficient-error heatmaps with respect to the MLSPG-sensitive linear HPROM coefficient trajectories. Columns are ordered as $\bm\mu^{(v)}$, $\bm\mu^{(1)}$, $\bm\mu^{(2)}$, and $\bm\mu^{(3)}$.}
\label{fig:mlspg-hprom-current-abs-heatmaps}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.98\textwidth]{Figures/mlspg_hprom_current/coeff_errors/mlspg_hprom_coeff_rel_heatmaps.png}
\caption{Current MLSPG-sensitive HPROM campaign: relative coefficient-error heatmaps with respect to the MLSPG-sensitive linear HPROM coefficient trajectories. Columns are ordered as $\bm\mu^{(v)}$, $\bm\mu^{(1)}$, $\bm\mu^{(2)}$, and $\bm\mu^{(3)}$.}
\label{fig:mlspg-hprom-current-rel-heatmaps}
\end{figure}


\subsection{Expanded Enriched MLSPG-Sensitive HPROM Campaign}

The expanded enriched campaign repeats Stage~3 training and online evaluation
after augmenting the baseline coefficient dataset with $36$ additional
linear-HPROM trajectories.  The enrichment is deliberately restricted to the
learning stage: the MLSPG-sensitive basis, HDM residual, time discretization,
and fixed linear Stage--2 ECSW rule remain unchanged.  The $36$ enrichment
points are generated by an LHS design with $18$ samples inside $\mathcal D$ and
$18$ samples in the $25\%$ expanded margin shown in
Figure~\ref{fig:parameter-domain-enrichment}.  The evaluation points, including
$\bm\mu^{(3)}$, are excluded from the enrichment set.  Thus, differences between
the non-enriched and expanded-enriched rows isolate the effect of additional
ROM-consistent coefficient data rather than a change in the trial space or in
the linear hyperreduction rule.

All expanded-enriched coefficient outputs used below are solver-side saved
$\mathbf q_N(t)$ arrays.  No coefficient in the expanded-enriched tables or
figures is obtained by least-squares post-projection of state snapshots.
Table~\ref{tab:mlspg-hprom-ext25-lhs36-training} reports the selected
checkpoint for each family.  The architecture choices are intentionally kept
close to the non-enriched production campaign: the wide MLP widths are reused
for the PROM-ANN and POD-NN-ROM maps, PROM-POD-AE uses a $n_z=10$ GELU
autoencoder, and POD-DL-ROM uses a $n_z=10$ latent dynamics map.  The final
PROM-ANN Case~1 checkpoint uses the same wide architecture but with GELU
activation; a same-width SiLU candidate had a low offline validation error but
was less robust in the intrusive online solve.

\input{tables/mlspg_hprom_ext25_lhs36_training_winners.tex}

Table~\ref{tab:mlspg-hprom-ext25-lhs36-errors} gives the expanded-enriched
online errors.  The fixed linear HPROM reference is repeated for orientation.
The most important change relative to the non-enriched campaign is the
stabilization of the learned maps on the extrapolatory point.  At the
extrapolatory point $\bm\mu^{(3)}$, POD-NN-ROM drops from $9.192\%$ to
$0.897\%$, POD-DL-ROM drops from $4.980\%$ to $0.867\%$, Case~2 with $n=10$
drops from $4.313\%$ to $0.904\%$, and Case~2 with $n=20$ drops from
$6.440\%$ to $0.914\%$.  PROM-ANN Case~1 also improves at
$\bm\mu^{(3)}$, from $1.563\%$ to $0.847\%$, after selecting the GELU
checkpoint; Case~3 improves from $1.438\%$ to $0.848\%$, and PROM-POD-AE
improves from $1.943\%$ to $0.885\%$.  On the original three evaluation points,
POD-NN-ROM and POD-DL-ROM become close to the fixed linear HPROM, with mean
errors $0.454\%$ and $0.449\%$, respectively, compared with $0.448\%$ for the
linear HPROM.  PROM-ANN Case~1 improves from a non-enriched mean of
$0.582\%$ to $0.461\%$, while the fragile Case~2 $n=20$ row improves from
$3.148\%$ to $0.453\%$.

\input{tables/mlspg_hprom_ext25_lhs36_errors.tex}

Table~\ref{tab:mlspg-hprom-ext25-lhs36-vs-current} compares the expanded
enriched campaign directly with the non-enriched baseline.  The table separates
the mean over the original three points from the extrapolatory stress-test
point.  This split is important because the expanded LHS design is not merely a
refinement of the original box; it is also intended to provide coefficient data
near and outside the original parameter boundary.

\input{tables/mlspg_hprom_ext25_lhs36_vs_current_errors.tex}

The expanded enrichment improves every learned row in the aggregate comparison
of Table~\ref{tab:mlspg-hprom-ext25-lhs36-vs-current}, but the magnitude of
the improvement is strongly model dependent.  The direct non-intrusive maps
benefit most from the expanded coverage, as expected for maps driven only by
$(\bm\mu,t)$.  PROM-ANN Case~2 also improves substantially, especially for
the $n=20$ split that was fragile in the non-enriched campaign.  After
enrichment, Case~1, Case~3, and the two Case~2 splits all lie close to the
fixed linear HPROM on the original three-point mean, while PROM-POD-AE remains
slightly less accurate on the in-domain off-grid points but gains clearly at
$\bm\mu^{(3)}$.  The Case~1 activation diagnostic indicates
that offline closure accuracy alone is not a sufficient selection criterion
for intrusive closure models: the final GELU checkpoint is retained because it
preserves the low offline error while producing a more stable online LSPG
trajectory than the same-width SiLU candidate.

The online dimensions, ECSW mesh sizes, and timings are reported in
Table~\ref{tab:mlspg-hprom-ext25-lhs36-hyperreduction}.  The learned intrusive
models use the same 2\% ECSW convention as the baseline production tables.  In
this expanded campaign, Case~1, Case~2 with $n=10$, Case~3, and PROM-POD-AE
retain $901$ ECSW elements, while Case~2 with $n=20$ retains $1801$ elements.
The linear HPROM row uses the fixed Stage--2 ECSW rule with $5975$ retained
elements.  Non-intrusive inference timings are CPU timings from saved run
summaries and should be read as conservative online costs.

\input{tables/mlspg_hprom_ext25_lhs36_hyperreduction.tex}

Figure~\ref{fig:mlspg-hprom-ext25-lhs36-overlays} compares the expanded
enriched models against HDM and the fixed linear HPROM at the four reported
evaluation points, including the extrapolatory stress-test point
$\bm\mu^{(3)}$.

\begin{figure}[H]
\centering
\includegraphics[width=0.98\textwidth]{Figures/mlspg_hprom_enrichment_ext25_lhs36/mlspg_hprom_ext25_lhs36_solution_overlays.png}
\caption{Expanded enriched MLSPG-sensitive campaign: HDM, fixed linear HPROM, expanded-enriched PROM-ANN HPROMs, expanded-enriched PROM-POD-AE with 2\% ECSW, expanded-enriched POD-NN-ROM, and expanded-enriched POD-DL-ROM. ROM curves are reconstructed from saved solver-side snapshots. Rows are ordered as $\bm\mu^{(v)}$, $\bm\mu^{(1)}$, $\bm\mu^{(2)}$, and $\bm\mu^{(3)}$.}
\label{fig:mlspg-hprom-ext25-lhs36-overlays}
\end{figure}

The expanded-enriched coefficient diagnostics in
Figures~\ref{fig:mlspg-hprom-ext25-lhs36-coeff-curves}--
\ref{fig:mlspg-hprom-ext25-lhs36-rel-heatmaps} use the fixed
MLSPG-sensitive linear HPROM coefficient trajectories as the reference at all
four evaluation points.  These diagnostics show the same qualitative picture
as the state errors: POD-NN-ROM and POD-DL-ROM become close to the linear
coefficient trajectories across the expanded test set, Case~2 benefits from the
additional parameter coverage, and Case~1 remains stable after switching to
the final GELU checkpoint.

\begin{figure}[H]
\centering
\includegraphics[width=0.98\textwidth]{Figures/mlspg_hprom_enrichment_ext25_lhs36/coeff_errors/mlspg_hprom_ext25_lhs36_coeff_abs_rel_all_points.png}
\caption{Expanded enriched MLSPG-sensitive campaign: per-coefficient absolute and relative errors with respect to the fixed MLSPG-sensitive linear HPROM coefficient trajectories. Panels are ordered as $\bm\mu^{(v)}$, $\bm\mu^{(1)}$, $\bm\mu^{(2)}$, and $\bm\mu^{(3)}$.}
\label{fig:mlspg-hprom-ext25-lhs36-coeff-curves}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.98\textwidth]{Figures/mlspg_hprom_enrichment_ext25_lhs36/coeff_errors/mlspg_hprom_ext25_lhs36_coeff_abs_heatmaps.png}
\caption{Expanded enriched MLSPG-sensitive campaign: absolute coefficient-error heatmaps with respect to the fixed MLSPG-sensitive linear HPROM coefficient trajectories. Columns are ordered as $\bm\mu^{(v)}$, $\bm\mu^{(1)}$, $\bm\mu^{(2)}$, and $\bm\mu^{(3)}$.}
\label{fig:mlspg-hprom-ext25-lhs36-abs-heatmaps}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.98\textwidth]{Figures/mlspg_hprom_enrichment_ext25_lhs36/coeff_errors/mlspg_hprom_ext25_lhs36_coeff_rel_heatmaps.png}
\caption{Expanded enriched MLSPG-sensitive campaign: relative coefficient-error heatmaps with respect to the fixed MLSPG-sensitive linear HPROM coefficient trajectories. Columns are ordered as $\bm\mu^{(v)}$, $\bm\mu^{(1)}$, $\bm\mu^{(2)}$, and $\bm\mu^{(3)}$.}
\label{fig:mlspg-hprom-ext25-lhs36-rel-heatmaps}
\end{figure}

\section{Shallow-Water Benchmark and Experimental Protocol}
\label{sec:shallowwater}

\subsection{Governing equations and parameterized initial condition}
The second benchmark is a 2D shallow-water model on a flat bottom, in
conservative form:
\begin{equation}
\frac{\partial \mathbf U}{\partial t}
+\frac{\partial \mathbf F(\mathbf U)}{\partial x}
+\frac{\partial \mathbf G(\mathbf U)}{\partial y}=0,
\qquad
\mathbf U=
\begin{bmatrix}
h\\ h u_x\\ h u_y
\end{bmatrix},
\end{equation}
with
\begin{equation}
\mathbf F(\mathbf U)=
\begin{bmatrix}
h u_x\\
h u_x^2+\tfrac{1}{2}gh^2\\
h u_x u_y
\end{bmatrix},
\qquad
\mathbf G(\mathbf U)=
\begin{bmatrix}
h u_y\\
h u_x u_y\\
h u_y^2+\tfrac{1}{2}gh^2
\end{bmatrix}.
\end{equation}

The parametric scenario is a collision of two Gaussian bumps in water depth:
\begin{equation}
h(x,y,0;\bm\mu)=h_0
+\mu_1\exp\!\left(-\frac{\|\mathbf x-\mathbf c_1\|_2^2}{2\sigma^2}\right)
+\mu_2\exp\!\left(-\frac{\|\mathbf x-\mathbf c_2\|_2^2}{2\sigma^2}\right),
\end{equation}
with zero initial velocity components ($u_x=u_y=0$), $h_0=1$, $\sigma=0.045$,
$\mathbf c_1=(0.30,0.50)$, $\mathbf c_2=(0.70,0.50)$, and
\begin{equation}
\bm\mu=(\mu_1,\mu_2)\in[0.06,0.14]\times[0.06,0.14].
\end{equation}
Reflective boundary conditions are imposed on all sides.

\subsection{Numerical setup}
The domain $\Omega=[0,1]\times[0,1]$ is discretized with $N_x=N_y=600$,
leading to $N=3N_xN_y$ unknowns. The HDM uses MUSCL reconstruction with
HLL/HLLC interface fluxes and an implicit BDF2 time integrator (BE startup).
We use a fixed time step $\Delta t=7\times10^{-5}$ up to $T_f=0.28$, yielding
$4001$ stored states per parameter value.

\subsection{Sampling protocol aligned with Burgers}
The staged workflow mirrors the Burgers protocol: baseline set of $9$ structured
parameter points ($3\times3$ grid), an HPROM-generated LHS enrichment set
($36$ points in the expanded Burgers campaign), Stage~1 POD basis $\mathbf V_{\mathrm{tot}}$ from HDM snapshots, Stage~2
ROM-consistent $\mathbf q_N$ dataset from linear HPROM in
$\mathbf V_{\mathrm{tot}}$, Stage~3 training of PROM-ANN (Cases~1--3),
PROM-POD-AE, POD-NN-ROM, and POD-DL-ROM, followed by online intrusive
evaluation with HPROM settings. This protocol preserves methodological
consistency across both benchmarks, so
that observed performance differences are attributable to model structure and
problem physics rather than to workflow changes.

\section{Scope of the Study}
Core model families are the linear HPROM reference, PROM-ANN (Cases~1--3),
PROM-POD-AE, POD-NN-ROM, and POD-DL-ROM. The baseline/enriched comparison is
retained as a controlled data axis. PG/PRNN-style variants are treated as
optional diagnostic tracks and are not part of the core comparative tables in
this manuscript.

\appendix

\section{Burgers Basis-Selection Diagnostics}
\label{app:basis-selection-diagnostics}

The $M$-regularized LSPG-sensitive basis was selected through two diagnostics.
The first diagnostic is the low/high transfer operator
$\mathbf T_{LH}$ introduced in \eqref{eq:low-high-transfer}.  Table
\ref{tab:low-high-transfer-summary} reports the singular-value statistics of
this operator over the three Burgers evaluation trajectories.  The
LSPG-sensitive basis reduces the mean value of
$\sigma_{\max}(\mathbf T_{LH})$ by approximately one half relative to the
Euclidean POD basis, and also reduces the 95th percentile and maximum values.
This supports the use of the modified metric as a robustness device for
intrusive nonlinear reduced models: if the nonlinear closure, decoder, or
surrogate produces errors in the unresolved part of the representation, the
resulting feedback into the resolved coordinates through the LSPG normal
equations is less amplifying.

\begin{table}[H]
\centering
\caption{Burgers low/high transfer diagnostic for basis selection. The table reports statistics of $\sigma_{\max}(\mathbf T_{LH})$ and of the corresponding $M$-weighted state gain $g_M$ over the original three evaluation trajectories.}
\label{tab:low-high-transfer-summary}
\resizebox{\textwidth}{!}{%
\input{MetricStudy/low_high_transfer/low_high_transfer_summary.tex}
}
\end{table}

As a complementary check, we perturb only the high-coordinate block of an
oracle reconstruction and measure the induced deviation from the linear
reduced trajectory.  This test is implemented with the Case~2 partition because
it isolates the most direct contamination mechanism: the high block is injected
independently of the online low-coordinate solve.  Table
\ref{tab:metric-oracle-diagnostic} reports averages over the original
verification point, two off-grid points, and five random seeds for the
perturbed cases.
The $M$-regularized LSPG-sensitive basis slightly increases the unperturbed
linear error in this preliminary test, but reduces the high-to-low
contamination measured against the linear reference, especially in the
resolved coordinates.  The intended role of the metric is broader than this
specific test: it provides a protective basis choice for nonlinear intrusive
models in which learned high coordinates or decoded manifold corrections may
contain approximation error.  This diagnostic is not used as a standalone
performance result; it supports the basis choice used in the main Burgers
campaign.

\begin{table}[H]
\centering
\caption{Oracle high-mode perturbation diagnostic for basis selection. Errors are averaged over $\bm\mu^{(v)}$, $\bm\mu^{(1)}$, $\bm\mu^{(2)}$; perturbed rows also average over five random seeds.}
\label{tab:metric-oracle-diagnostic}
\resizebox{\textwidth}{!}{
\begin{tabular}{lccccc}
\toprule
Basis metric &
Unperturbed $\varepsilon_u$ vs HDM (\%) &
$1\%$ pert., $\varepsilon_u$ vs linear (\%) &
$1\%$ pert., $\varepsilon_q^{\mathrm{low}}$ vs linear (\%) &
$2\%$ pert., $\varepsilon_u$ vs linear (\%) &
$2\%$ pert., $\varepsilon_q^{\mathrm{low}}$ vs linear (\%) \\
\midrule
Euclidean POD & 0.441 & 0.091 & 0.014 & 0.183 & 0.045 \\
$M$-regularized LSPG-sensitive POD & 0.448 & 0.088 & 0.012 & 0.177 & 0.035 \\
\bottomrule
\end{tabular}
}
\end{table}

\section{Case 2 Trimmed-Closure Diagnostic}
\label{app:case2-trimmed-diagnostic}

The non-enriched results show that PROM-ANN Case~2 with $n=10$ is more robust
than the independently trained Case~2 model with $n=20$, especially away from
the verification point.  To test whether this behavior is caused primarily by
the larger online split or by the learned parameter--time tail map itself, we
ran an additional diagnostic.  The diagnostic reuses the trained $n=10$ Case~2
map
\begin{equation}
(\mu_1,\mu_2,t)\mapsto(q_{11},\ldots,q_{151}),
\end{equation}
discards its first ten secondary outputs, and injects only
$(q_{21},\ldots,q_{151})$ in an $n=20$ online HPROM solve.  Thus, the online
unknown is enlarged from $10$ to $20$ primary coordinates, but the high-mode
closure is inherited from the better-performing $n=10$ map rather than from an
independently trained $n=20$ network.  The same diagnostic is also run at the
extrapolatory point $\bm\mu^{(3)}$ introduced in the main text.  This is a
diagnostic variant only; it is not included in the main selected-model
comparison.

\input{tables/mlspg_hprom_case2_trimmed_from_np10_diagnostic.tex}

Figure~\ref{fig:case2-trimmed-from-np10-diagnostic} visualizes the same
comparison.  The trimmed run substantially reduces the degradation of the
native $n=20$ Case~2 model at the two off-grid points and at
$\bm\mu^{(3)}$, which indicates that the independently trained $n=20$
parameter--time closure is the main source of the poor generalization.
However, the trimmed run is not uniformly better than the native $n=10$ solve
on the original in-domain points and is more expensive because the online
unknown dimension is larger and the diagnostic ECSW rule retains more
elements.  This supports keeping the trimmed model in the appendix rather than
promoting it to the main baseline table.

\begin{figure}[H]
\centering
\includegraphics[width=0.95\textwidth]{Figures/mlspg_hprom_current/case2_trimmed_diagnostic/case2_trimmed_from_np10_error_time_bars.png}
\caption{Case~2 trimmed-closure diagnostic in the non-enriched MLSPG-sensitive campaign. The trimmed variant uses an $n=20$ online solve but reuses the trained $n=10$ Case~2 map after discarding the first ten predicted secondary coordinates. The extrapolatory point $\bm\mu^{(3)}$ is reported separately from the in-domain mean.}
\label{fig:case2-trimmed-from-np10-diagnostic}
\end{figure}

\bibliographystyle{unsrt}
\bibliography{bibliography}

\end{document}
